# PixelArt Studio Phase 2 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 Phase 2 增强功能：多边形/手绘框选、反向选区、自定义调色板、自定义预设持久化、人物像素风（4种模式）、Sprite Sheet 导出、撤销/重做、Web Worker 优化、缩略图预览优化。

**Architecture:** 延续 Phase 1 模块化引擎架构。引擎层零 React 依赖，新增 `person/` 子模块和 `worker.ts`；UI 层通过 Zustand 扩展状态。自定义预设通过 localStorage 持久化，人物像素风通过 Canvas 像素操作实现（Cutout 依赖 @mediapipe/selfie-segmentation CDN 懒加载）。

**Tech Stack:** React 19 + Vite 6 + TypeScript 5.7 + Tailwind CSS 4 + Zustand 5 + Vitest + @mediapipe/selfie-segmentation

---

## 文件结构预览

```
src/
├── engine/
│   ├── worker.ts              # Web Worker 入口：接收 ImageData + options，返回 ImageData
│   ├── types.ts               # 扩展：添加 EdgeEnhanceOptions, PersonMode, SpriteOptions
│   ├── pipeline.ts            # 扩展：支持 edgeEnhance 和 person 模式
│   ├── algorithms/
│   │   └── edge-enhance.ts    # 轮廓增强算法（Sobel）
│   ├── person/
│   │   ├── normal.ts          # Normal 模式：直接透传 pipeline
│   │   ├── cutout.ts          # Cutout 模式：mediapipe 分割 + 背景处理
│   │   ├── style.ts           # Style 模式：降采样 + 边缘增强 + 固定调色板
│   │   └── sprite.ts          # Sprite 模式：多图统一像素化
│   ├── selection/
│   │   ├── shapes.ts          # 扩展：Polygon + Freehand
│   │   └── mask.ts            # 扩展：支持 polygon 和 freehand 的 isPointInPath
│   └── export/
│       └── sprite-sheet.ts    # Sprite Sheet 拼接导出
├── store/
│   ├── index.ts               # 扩展：添加 selection.invert, history, customPresets, personMode 等
│   └── storage.ts             # localStorage 适配器（自定义预设）
├── components/
│   ├── canvas/
│   │   └── SelectionOverlay.tsx   # 扩展：多边形点选 + 自由手绘交互
│   ├── params/
│   │   ├── CustomPaletteEditor.tsx    # 调色板编辑器：添加/删除/修改颜色
│   │   ├── MaskInvertToggle.tsx       # 反向选区开关
│   │   ├── PersonModeSelector.tsx     # 人物模式选择器
│   │   └── ParamsPanel.tsx            # 更新：集成新组件
│   ├── toolbar/
│   │   └── HistoryButtons.tsx         # 撤销/重做按钮
│   └── modals/
│       └── SpriteSheetModal.tsx       # Sprite Sheet 导出弹窗
├── hooks/
│   ├── useHistory.ts          # 撤销/重做 Hook
│   ├── useSelection.ts        # 框选交互逻辑封装
│   └── usePersonMode.ts       # 人物模式处理 Hook
└── utils/
    └── canvas.ts              # Canvas 辅助函数（新增）
```

---

## Task 1: 扩展 Shape 类型 — 多边形与自由手绘

**Files:**
- Modify: `src/engine/selection/shapes.ts`
- Test: `tests/engine/selection/shapes.test.ts`

**Context:** Phase 1 已实现 Rectangle 和 Circle。Phase 2 需要添加 Polygon（多边形点选）和 Freehand（自由手绘路径）。多边形由顶点数组定义，手绘由路径点数组定义。两者都用 Canvas `isPointInPath` 判断点是否在内部。

- [ ] **Step 1: 编写形状测试**

```typescript
// tests/engine/selection/shapes.test.ts
import { describe, it, expect } from 'vitest';
import { createRectangle, createCircle, createPolygon, createFreehand, type Polygon, type Freehand } from '../../../src/engine/selection/shapes';

describe('shapes', () => {
  it('createRectangle', () => {
    const r = createRectangle({ x: 10, y: 10 }, { x: 30, y: 40 });
    expect(r).toEqual({ type: 'rectangle', x: 10, y: 10, width: 20, height: 30 });
  });

  it('createCircle', () => {
    const c = createCircle({ x: 50, y: 50 }, { x: 60, y: 50 });
    expect(c).toEqual({ type: 'circle', cx: 50, cy: 50, radius: 10 });
  });

  it('createPolygon with 3 points', () => {
    const p = createPolygon([{ x: 0, y: 0 }, { x: 10, y: 0 }, { x: 5, y: 10 }]);
    expect(p.type).toBe('polygon');
    expect(p.points).toHaveLength(3);
  });

  it('createFreehand with path points', () => {
    const f = createFreehand([{ x: 0, y: 0 }, { x: 5, y: 5 }, { x: 10, y: 0 }]);
    expect(f.type).toBe('freehand');
    expect(f.path).toHaveLength(3);
  });

  it('polygon point is inside', () => {
    // Use Canvas isPointInPath via a helper — tested in mask.test.ts
    // This test just verifies the shape structure
    const p: Polygon = { type: 'polygon', points: [{ x: 0, y: 0 }, { x: 10, y: 0 }, { x: 5, y: 10 }] };
    expect(p.points[1].x).toBe(10);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/engine/selection/shapes.test.ts`
Expected: FAIL — `createPolygon` and `createFreehand` not defined

- [ ] **Step 3: 扩展 shapes.ts**

```typescript
// src/engine/selection/shapes.ts
export type ShapeType = 'rectangle' | 'circle' | 'polygon' | 'freehand';

export interface Point {
  x: number;
  y: number;
}

export interface Rectangle {
  type: 'rectangle';
  x: number;
  y: number;
  width: number;
  height: number;
}

export interface Circle {
  type: 'circle';
  cx: number;
  cy: number;
  radius: number;
}

export interface Polygon {
  type: 'polygon';
  points: Point[];
}

export interface Freehand {
  type: 'freehand';
  path: Point[];
}

export type Shape = Rectangle | Circle | Polygon | Freehand;

export function createRectangle(start: Point, end: Point): Rectangle {
  const x = Math.min(start.x, end.x);
  const y = Math.min(start.y, end.y);
  const width = Math.abs(end.x - start.x);
  const height = Math.abs(end.y - start.y);
  return { type: 'rectangle', x, y, width, height };
}

export function createCircle(start: Point, end: Point): Circle {
  const cx = start.x;
  const cy = start.y;
  const radius = Math.sqrt((end.x - start.x) ** 2 + (end.y - start.y) ** 2);
  return { type: 'circle', cx, cy, radius };
}

export function createPolygon(points: Point[]): Polygon {
  return { type: 'polygon', points };
}

export function createFreehand(path: Point[]): Freehand {
  return { type: 'freehand', path };
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/engine/selection/shapes.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/selection/shapes.ts tests/engine/selection/shapes.test.ts
git commit -m "feat: 扩展 Shape 类型支持多边形和自由手绘"
```

---

## Task 2: 扩展 Mask 引擎 — 支持多边形与手绘

**Files:**
- Modify: `src/engine/selection/mask.ts`
- Test: `tests/engine/selection/mask.test.ts`

**Context:** 使用 Canvas 2D 的 `isPointInPath` 判断点是否在多边形或手绘路径内。创建一个离屏 canvas，构建 Path2D，然后用 `isPointInPath` 测试每个像素点。

- [ ] **Step 1: 编写 mask 测试**

```typescript
// tests/engine/selection/mask.test.ts
import { describe, it, expect } from 'vitest';
import { createMask, applyMask } from '../../../src/engine/selection/mask';
import type { Polygon, Freehand } from '../../../src/engine/selection/shapes';

function createImageData(width: number, height: number, fill: [number, number, number]): ImageData {
  const data = new Uint8ClampedArray(width * height * 4);
  for (let i = 0; i < data.length; i += 4) {
    data[i] = fill[0];
    data[i + 1] = fill[1];
    data[i + 2] = fill[2];
    data[i + 3] = 255;
  }
  return new ImageData(data, width, height);
}

describe('createMask', () => {
  it('rectangle mask marks inside pixels', () => {
    const shape = { type: 'rectangle' as const, x: 1, y: 1, width: 2, height: 2 };
    const mask = createMask(shape, 4, 4, false);
    // Center 2x2 should be 1, rest 0
    expect(mask[5]).toBe(1); // (1,1)
    expect(mask[0]).toBe(0); // (0,0)
  });

  it('polygon mask with triangle', () => {
    const shape: Polygon = {
      type: 'polygon',
      points: [{ x: 2, y: 0 }, { x: 4, y: 4 }, { x: 0, y: 4 }],
    };
    const mask = createMask(shape, 5, 5, false);
    // Point inside triangle (2, 2) should be 1
    expect(mask[2 * 5 + 2]).toBe(1);
    // Point outside (0, 0) should be 0
    expect(mask[0]).toBe(0);
  });

  it('freehand mask with simple path', () => {
    const shape: Freehand = {
      type: 'freehand',
      path: [{ x: 0, y: 0 }, { x: 4, y: 0 }, { x: 4, y: 4 }, { x: 0, y: 4 }],
    };
    const mask = createMask(shape, 5, 5, false);
    // Point inside (2, 2) should be 1
    expect(mask[2 * 5 + 2]).toBe(1);
  });

  it('invert flips mask values', () => {
    const shape = { type: 'rectangle' as const, x: 0, y: 0, width: 2, height: 2 };
    const mask = createMask(shape, 4, 4, true);
    expect(mask[0]).toBe(0); // inside -> 0
    expect(mask[3]).toBe(1); // outside -> 1
  });
});

describe('applyMask', () => {
  it('applies processed pixels where mask is 1', () => {
    const source = createImageData(2, 2, [0, 0, 0]);
    const processed = createImageData(2, 2, [255, 255, 255]);
    const mask = new Uint8Array([1, 0, 0, 1]);
    const result = applyMask(source, processed, mask);
    expect(result.data[0]).toBe(255); // masked
    expect(result.data[4]).toBe(0);   // unmasked
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run tests/engine/selection/mask.test.ts`
Expected: FAIL — Polygon/Freehand cases fail because `isPointInShape` doesn't handle them

- [ ] **Step 3: 扩展 mask.ts**

```typescript
// src/engine/selection/mask.ts
import type { Shape, Point } from './shapes';

// Reusable offscreen canvas for path testing
let _pathCanvas: HTMLCanvasElement | null = null;
let _pathCtx: CanvasRenderingContext2D | null = null;

function getPathContext(): CanvasRenderingContext2D {
  if (!_pathCtx) {
    _pathCanvas = document.createElement('canvas');
    _pathCanvas.width = 1;
    _pathCanvas.height = 1;
    _pathCtx = _pathCanvas.getContext('2d')!;
  }
  return _pathCtx;
}

export function createMask(shape: Shape, width: number, height: number, invert: boolean): Uint8Array {
  const mask = new Uint8Array(width * height);

  for (let y = 0; y < height; y++) {
    for (let x = 0; x < width; x++) {
      const idx = y * width + x;
      const inside = isPointInShape(x, y, shape);
      mask[idx] = invert ? (inside ? 0 : 1) : (inside ? 1 : 0);
    }
  }

  return mask;
}

function isPointInShape(x: number, y: number, shape: Shape): boolean {
  switch (shape.type) {
    case 'rectangle':
      return x >= shape.x && x <= shape.x + shape.width &&
             y >= shape.y && y <= shape.y + shape.height;
    case 'circle': {
      const dx = x - shape.cx;
      const dy = y - shape.cy;
      return dx * dx + dy * dy <= shape.radius * shape.radius;
    }
    case 'polygon':
      return isPointInPolygonPath(x, y, shape.points);
    case 'freehand':
      return isPointInFreehandPath(x, y, shape.path);
  }
}

function isPointInPolygonPath(x: number, y: number, points: Point[]): boolean {
  if (points.length < 3) return false;
  const ctx = getPathContext();
  ctx.beginPath();
  ctx.moveTo(points[0].x, points[0].y);
  for (let i = 1; i < points.length; i++) {
    ctx.lineTo(points[i].x, points[i].y);
  }
  ctx.closePath();
  return ctx.isPointInPath(x, y);
}

function isPointInFreehandPath(x: number, y: number, path: Point[]): boolean {
  if (path.length < 3) return false;
  const ctx = getPathContext();
  ctx.beginPath();
  ctx.moveTo(path[0].x, path[0].y);
  for (let i = 1; i < path.length; i++) {
    ctx.lineTo(path[i].x, path[i].y);
  }
  ctx.closePath();
  return ctx.isPointInPath(x, y);
}

export function applyMask(
  source: ImageData,
  processed: ImageData,
  mask: Uint8Array
): ImageData {
  const { width, height, data: srcData } = source;
  const { data: procData } = processed;
  const result = new Uint8ClampedArray(srcData);

  for (let i = 0; i < mask.length; i++) {
    if (mask[i] === 1) {
      const idx = i * 4;
      result[idx] = procData[idx];
      result[idx + 1] = procData[idx + 1];
      result[idx + 2] = procData[idx + 2];
    }
  }

  return new ImageData(result, width, height);
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run tests/engine/selection/mask.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/engine/selection/mask.ts tests/engine/selection/mask.test.ts
git commit -m "feat: Mask 引擎支持多边形和自由手绘选区"
```

---

## Task 3: SelectionOverlay 扩展 — 多边形与手绘交互

**Files:**
- Modify: `src/components/canvas/SelectionOverlay.tsx`
- Modify: `src/App.tsx`

**Context:** 多边形模式：点击添加顶点，顶点间自动连线，双击闭合。手绘模式：按住鼠标拖拽记录路径点，释放后自动闭合。需要更新 App.tsx 的 `SelectionTool` 类型以包含新工具。

- [ ] **Step 1: 更新 App.tsx 的 SelectionTool 类型**

```typescript
// src/App.tsx
import { useState } from 'react';
import { Layout } from './components/layout/Layout';
import { Toolbar } from './components/toolbar/Toolbar';
import { CanvasViewport } from './components/canvas/CanvasViewport';
import { ParamsPanel } from './components/params/ParamsPanel';

export type SelectionTool = 'rectangle' | 'circle' | 'polygon' | 'freehand' | null;

function App() {
  const [activeTool, setActiveTool] = useState<SelectionTool>(null);

  return (
    <Layout>
      <Toolbar onSelectTool={setActiveTool} activeTool={activeTool} />
      <CanvasViewport selectionTool={activeTool} />
      <ParamsPanel />
    </Layout>
  );
}

export default App;
```

- [ ] **Step 2: 重写 SelectionOverlay 支持多边形和手绘**

```tsx
// src/components/canvas/SelectionOverlay.tsx
import { useRef, useState, useCallback, useEffect } from 'react';
import type { Point, Shape } from '../../engine/selection/shapes';
import { createRectangle, createCircle, createPolygon, createFreehand } from '../../engine/selection/shapes';

export type SelectionTool = 'rectangle' | 'circle' | 'polygon' | 'freehand' | null;

interface SelectionOverlayProps {
  width: number;
  height: number;
  tool: SelectionTool;
  onShapeCreated: (shape: Shape | null) => void;
}

export function SelectionOverlay({ width, height, tool, onShapeCreated }: SelectionOverlayProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const [isDrawing, setIsDrawing] = useState(false);
  const [startPoint, setStartPoint] = useState<Point | null>(null);
  const [currentShape, setCurrentShape] = useState<Shape | null>(null);
  const [polygonPoints, setPolygonPoints] = useState<Point[]>([]);
  const [freehandPath, setFreehandPath] = useState<Point[]>([]);

  const getPoint = useCallback((e: React.MouseEvent<HTMLCanvasElement>): Point => {
    const canvas = canvasRef.current!;
    const rect = canvas.getBoundingClientRect();
    const scaleX = canvas.width / rect.width;
    const scaleY = canvas.height / rect.height;
    return {
      x: (e.clientX - rect.left) * scaleX,
      y: (e.clientY - rect.top) * scaleY,
    };
  }, []);

  const clearCanvas = useCallback(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext('2d')!;
    ctx.clearRect(0, 0, canvas.width, canvas.height);
  }, []);

  const drawShape = useCallback((ctx: CanvasRenderingContext2D, shape: Shape) => {
    ctx.strokeStyle = '#22c55e';
    ctx.lineWidth = 2;
    ctx.setLineDash([5, 5]);
    ctx.fillStyle = 'rgba(34, 197, 94, 0.15)';

    if (shape.type === 'rectangle') {
      ctx.strokeRect(shape.x, shape.y, shape.width, shape.height);
      ctx.fillRect(shape.x, shape.y, shape.width, shape.height);
    } else if (shape.type === 'circle') {
      ctx.beginPath();
      ctx.arc(shape.cx, shape.cy, shape.radius, 0, Math.PI * 2);
      ctx.stroke();
      ctx.fill();
    } else if (shape.type === 'polygon') {
      if (shape.points.length < 2) return;
      ctx.beginPath();
      ctx.moveTo(shape.points[0].x, shape.points[0].y);
      for (let i = 1; i < shape.points.length; i++) {
        ctx.lineTo(shape.points[i].x, shape.points[i].y);
      }
      ctx.closePath();
      ctx.stroke();
      ctx.fill();
    } else if (shape.type === 'freehand') {
      if (shape.path.length < 2) return;
      ctx.beginPath();
      ctx.moveTo(shape.path[0].x, shape.path[0].y);
      for (let i = 1; i < shape.path.length; i++) {
        ctx.lineTo(shape.path[i].x, shape.path[i].y);
      }
      ctx.closePath();
      ctx.stroke();
      ctx.fill();
    }

    ctx.setLineDash([]);
  }, []);

  const redraw = useCallback((shape: Shape | null, tempPoints?: Point[]) => {
    clearCanvas();
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext('2d')!;

    if (shape) {
      drawShape(ctx, shape);
    }

    // Draw polygon vertices
    if (tempPoints && tempPoints.length > 0) {
      ctx.fillStyle = '#22c55e';
      for (const p of tempPoints) {
        ctx.beginPath();
        ctx.arc(p.x, p.y, 3, 0, Math.PI * 2);
        ctx.fill();
      }
      // Draw lines between vertices
      if (tempPoints.length > 1) {
        ctx.strokeStyle = '#22c55e';
        ctx.lineWidth = 1;
        ctx.setLineDash([3, 3]);
        ctx.beginPath();
        ctx.moveTo(tempPoints[0].x, tempPoints[0].y);
        for (let i = 1; i < tempPoints.length; i++) {
          ctx.lineTo(tempPoints[i].x, tempPoints[i].y);
        }
        ctx.stroke();
        ctx.setLineDash([]);
      }
    }
  }, [clearCanvas, drawShape]);

  // --- Rectangle / Circle handlers ---
  const handleMouseDownDrag = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (!tool || tool === 'polygon' || tool === 'freehand') return;
    const point = getPoint(e);
    setIsDrawing(true);
    setStartPoint(point);
    setCurrentShape(null);
  }, [tool, getPoint]);

  const handleMouseMoveDrag = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (!isDrawing || !startPoint || !tool || tool === 'polygon' || tool === 'freehand') return;

    const currentPoint = getPoint(e);
    let shape: Shape;

    if (tool === 'rectangle') {
      shape = createRectangle(startPoint, currentPoint);
    } else {
      shape = createCircle(startPoint, currentPoint);
    }

    setCurrentShape(shape);
    redraw(shape);
  }, [isDrawing, startPoint, tool, getPoint, redraw]);

  const handleMouseUpDrag = useCallback(() => {
    if (!isDrawing) return;
    setIsDrawing(false);

    if (currentShape) {
      onShapeCreated(currentShape);
    }
  }, [isDrawing, currentShape, onShapeCreated]);

  // --- Polygon handlers ---
  const handlePolygonClick = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (tool !== 'polygon') return;
    const point = getPoint(e);
    const newPoints = [...polygonPoints, point];
    setPolygonPoints(newPoints);
    redraw(null, newPoints);
  }, [tool, getPoint, polygonPoints, redraw]);

  const handlePolygonDoubleClick = useCallback(() => {
    if (tool !== 'polygon' || polygonPoints.length < 3) return;
    const shape = createPolygon(polygonPoints);
    setCurrentShape(shape);
    setPolygonPoints([]);
    redraw(shape);
    onShapeCreated(shape);
  }, [tool, polygonPoints, redraw, onShapeCreated]);

  // --- Freehand handlers ---
  const handleFreehandMouseDown = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (tool !== 'freehand') return;
    const point = getPoint(e);
    setIsDrawing(true);
    setFreehandPath([point]);
  }, [tool, getPoint]);

  const handleFreehandMouseMove = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (!isDrawing || tool !== 'freehand') return;
    const point = getPoint(e);
    const newPath = [...freehandPath, point];
    setFreehandPath(newPath);
    const shape = createFreehand(newPath);
    setCurrentShape(shape);
    redraw(shape);
  }, [isDrawing, tool, getPoint, freehandPath, redraw]);

  const handleFreehandMouseUp = useCallback(() => {
    if (!isDrawing || tool !== 'freehand') return;
    setIsDrawing(false);
    if (freehandPath.length >= 3 && currentShape) {
      onShapeCreated(currentShape);
    }
  }, [isDrawing, tool, freehandPath, currentShape, onShapeCreated]);

  // Unified mouse handlers
  const handleMouseDown = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (tool === 'polygon') {
      handlePolygonClick(e);
    } else if (tool === 'freehand') {
      handleFreehandMouseDown(e);
    } else {
      handleMouseDownDrag(e);
    }
  }, [tool, handlePolygonClick, handleFreehandMouseDown, handleMouseDownDrag]);

  const handleMouseMove = useCallback((e: React.MouseEvent<HTMLCanvasElement>) => {
    if (tool === 'freehand') {
      handleFreehandMouseMove(e);
    } else {
      handleMouseMoveDrag(e);
    }
  }, [tool, handleFreehandMouseMove, handleMouseMoveDrag]);

  const handleMouseUp = useCallback(() => {
    if (tool === 'freehand') {
      handleFreehandMouseUp();
    } else {
      handleMouseUpDrag();
    }
  }, [tool, handleFreehandMouseUp, handleMouseUpDrag]);

  // Clear when tool changes
  useEffect(() => {
    clearCanvas();
    setCurrentShape(null);
    setPolygonPoints([]);
    setFreehandPath([]);
    setIsDrawing(false);
    setStartPoint(null);
    onShapeCreated(null);
  }, [tool, clearCanvas, onShapeCreated]);

  // Keyboard: Delete/Backspace to clear
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.key === 'Delete' || e.key === 'Backspace') {
        clearCanvas();
        setCurrentShape(null);
        setPolygonPoints([]);
        setFreehandPath([]);
        onShapeCreated(null);
      }
    };

    if (tool) {
      window.addEventListener('keydown', handleKeyDown);
      return () => window.removeEventListener('keydown', handleKeyDown);
    }
  }, [tool, clearCanvas, onShapeCreated]);

  if (!tool) return null;

  const cursorStyle = tool === 'polygon' ? 'crosshair' : tool === 'freehand' ? 'crosshair' : 'crosshair';

  return (
    <canvas
      ref={canvasRef}
      width={width}
      height={height}
      onMouseDown={handleMouseDown}
      onMouseMove={handleMouseMove}
      onMouseUp={handleMouseUp}
      onMouseLeave={handleMouseUp}
      onDoubleClick={handlePolygonDoubleClick}
      className="absolute top-0 left-0"
      style={{ pointerEvents: 'auto', cursor: cursorStyle }}
    />
  );
}
```

- [ ] **Step 3: 更新 Toolbar 添加新工具按钮**

```tsx
// src/components/toolbar/Toolbar.tsx
import { useState } from 'react';
import { ExportButton } from './ExportButton';

export type SelectionTool = 'rectangle' | 'circle' | 'polygon' | 'freehand' | null;

interface ToolbarProps {
  onSelectTool: (tool: SelectionTool) => void;
  activeTool: SelectionTool;
}

export function Toolbar({ onSelectTool, activeTool }: ToolbarProps) {
  const tools: { id: SelectionTool; label: string; title: string }[] = [
    { id: 'rectangle', label: '□', title: '矩形框选' },
    { id: 'circle', label: '○', title: '圆形框选' },
    { id: 'polygon', label: '◇', title: '多边形框选' },
    { id: 'freehand', label: '✎', title: '自由手绘' },
  ];

  return (
    <div className="w-14 bg-retro-panel border-r-2 border-retro-border flex flex-col items-center py-3 gap-2">
      <span className="text-retro-muted text-[10px] font-pixel mb-2">TOOLS</span>

      {tools.map((t) => (
        <button
          key={t.id}
          onClick={() => onSelectTool(activeTool === t.id ? null : t.id)}
          className={`w-10 h-10 border flex items-center justify-center transition-colors text-xs ${
            activeTool === t.id
              ? 'border-retro-primary text-retro-primary bg-retro-primary/10'
              : 'border-retro-border text-retro-muted hover:text-retro-text hover:border-retro-text'
          }`}
          title={t.title}
        >
          {t.label}
        </button>
      ))}

      <div className="flex-1" />
      <ExportButton />
    </div>
  );
}
```

- [ ] **Step 4: TypeScript 编译检查**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 5: 验证框选功能**

Run: `npm run dev`
Expected:
1. 左侧工具栏有矩形、圆形、多边形、自由手绘四个按钮
2. 多边形模式：点击添加顶点，显示绿色圆点和连线，双击闭合
3. 手绘模式：按住鼠标拖拽绘制，释放后自动闭合
4. 所有模式下选区内应用像素化，选区外保持原样
5. Delete/Backspace 清除选区

- [ ] **Step 6: Commit**

```bash
git add src/components/canvas/SelectionOverlay.tsx src/components/toolbar/Toolbar.tsx src/App.tsx
git commit -m "feat: 实现多边形点选和自由手绘框选"
```

---

## Task 4: 反向选区 (Mask Invert Toggle)

**Files:**
- Create: `src/components/params/MaskInvertToggle.tsx`
- Modify: `src/components/params/ParamsPanel.tsx`
- Modify: `src/store/index.ts`
- Modify: `src/components/canvas/ImageCanvas.tsx`

**Context:** 添加一个 Toggle 开关，控制选区遮罩是否反转。默认：选区内像素化，选区外原图。反转：选区外像素化，选区内原图。状态存入 Zustand store，传递给 `createMask` 的 `invert` 参数。

- [ ] **Step 1: 扩展 Store 添加 selection.invert**

```typescript
// src/store/index.ts — 修改部分
import { create } from 'zustand';
import type { QuantizeMethod } from '../engine/types';

export type Algorithm = 'nearest' | 'average';

export interface AppState {
  // Image
  originalImage: HTMLImageElement | null;
  setOriginalImage: (image: HTMLImageElement | null) => void;

  // Params
  params: {
    blockSize: number;
    algorithm: Algorithm;
    quantizeMethod: QuantizeMethod;
    maxColors: number;
  };
  setParams: (params: Partial<AppState['params']>) => void;

  // Preset
  presetId: string | null;
  setPresetId: (id: string | null) => void;

  // Selection
  selection: {
    invert: boolean;
  };
  setSelectionInvert: (invert: boolean) => void;

  // Compare
  ui: {
    showCompare: boolean;
    compareMode: 'split' | 'slider';
  };
  setCompareMode: (show: boolean) => void;
  setCompareType: (mode: 'split' | 'slider') => void;

  // Processing state
  isProcessing: boolean;
  setIsProcessing: (processing: boolean) => void;
}

export const useAppStore = create<AppState>((set) => ({
  originalImage: null,
  setOriginalImage: (image) => set({ originalImage: image }),

  params: {
    blockSize: 8,
    algorithm: 'nearest',
    quantizeMethod: 'none' as QuantizeMethod,
    maxColors: 16,
  },
  setParams: (newParams) =>
    set((state) => ({
      params: { ...state.params, ...newParams },
    })),

  presetId: null,
  setPresetId: (id) => set({ presetId: id }),

  selection: {
    invert: false,
  },
  setSelectionInvert: (invert) =>
    set((state) => ({
      selection: { ...state.selection, invert },
    })),

  ui: {
    showCompare: false,
    compareMode: 'split',
  },
  setCompareMode: (show) =>
    set((state) => ({
      ui: { ...state.ui, showCompare: show },
    })),
  setCompareType: (mode) =>
    set((state) => ({
      ui: { ...state.ui, compareMode: mode },
    })),

  isProcessing: false,
  setIsProcessing: (processing) => set({ isProcessing: processing }),
}));
```

- [ ] **Step 2: 创建 MaskInvertToggle 组件**

```tsx
// src/components/params/MaskInvertToggle.tsx
import { useAppStore } from '../../store';

export function MaskInvertToggle() {
  const invert = useAppStore((s) => s.selection.invert);
  const setSelectionInvert = useAppStore((s) => s.setSelectionInvert);

  return (
    <div className="space-y-2">
      <h3 className="text-retro-muted text-xs font-pixel">SELECTION</h3>
      <button
        onClick={() => setSelectionInvert(!invert)}
        className={`w-full py-1.5 text-xs font-terminal border transition-colors ${
          invert
            ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
            : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
        }`}
      >
        {invert ? '反转选区: 开' : '反转选区: 关'}
      </button>
      <p className="text-[10px] text-retro-muted font-terminal">
        {invert ? '选区外像素化，选区内保持原图' : '选区内像素化，选区外保持原图'}
      </p>
    </div>
  );
}
```

- [ ] **Step 3: 更新 ImageCanvas 读取 invert 状态**

```tsx
// src/components/canvas/ImageCanvas.tsx — 修改 useEffect 中的 mask 创建部分
// 在组件顶部添加 invert 的读取：
// const invert = useAppStore((s) => s.selection.invert);
//
// 在 mask 创建处将 false 改为 invert：
// const mask = createMask(shape, sourceData.width, sourceData.height, invert);
```

完整修改后的 ImageCanvas.tsx：

```tsx
// src/components/canvas/ImageCanvas.tsx
import { useEffect, useRef, useState } from 'react';
import { useAppStore } from '../../store';
import { processImage } from '../../engine';
import { createPresetRegistry } from '../../engine/presets';
import { imageToImageData, imageDataToCanvas } from '../../utils/image';
import { createMask, applyMask } from '../../engine/selection/mask';
import type { Shape } from '../../engine/selection/shapes';
import { SelectionOverlay } from './SelectionOverlay';
import type { SelectionTool } from './SelectionOverlay';

const registry = createPresetRegistry();

interface ImageCanvasProps {
  selectionTool?: SelectionTool;
}

export function ImageCanvas({ selectionTool }: ImageCanvasProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const originalImage = useAppStore((s) => s.originalImage);
  const params = useAppStore((s) => s.params);
  const presetId = useAppStore((s) => s.presetId);
  const invert = useAppStore((s) => s.selection.invert);
  const setIsProcessing = useAppStore((s) => s.setIsProcessing);
  const [shape, setShape] = useState<Shape | null>(null);

  useEffect(() => {
    if (!originalImage || !canvasRef.current) return;

    setIsProcessing(true);

    requestAnimationFrame(() => {
      const sourceData = imageToImageData(originalImage);

      let options = {
        downsample: { blockSize: params.blockSize, algorithm: params.algorithm },
        quantize: {
          method: params.quantizeMethod,
          maxColors: params.maxColors,
        },
      };

      if (presetId) {
        const preset = registry.get(presetId);
        if (preset) {
          options = {
            downsample: preset.config.downsample,
            quantize: preset.config.quantize,
          };
        }
      }

      let result = processImage(sourceData, options);

      if (shape) {
        const mask = createMask(shape, sourceData.width, sourceData.height, invert);
        result = applyMask(sourceData, result, mask);
      }

      const resultCanvas = imageDataToCanvas(result);
      const canvas = canvasRef.current!;
      const ctx = canvas.getContext('2d')!;

      canvas.width = resultCanvas.width;
      canvas.height = resultCanvas.height;
      ctx.drawImage(resultCanvas, 0, 0);

      setIsProcessing(false);
    });
  }, [originalImage, params, presetId, setIsProcessing, shape, invert]);

  if (!originalImage) return null;

  return (
    <div className="relative inline-block">
      <canvas
        ref={canvasRef}
        className="max-w-full max-h-full object-contain"
        style={{ imageRendering: 'pixelated' }}
      />
      <SelectionOverlay
        width={originalImage.naturalWidth}
        height={originalImage.naturalHeight}
        tool={selectionTool ?? null}
        onShapeCreated={setShape}
      />
    </div>
  );
}
```

- [ ] **Step 4: 更新 ParamsPanel 添加 MaskInvertToggle**

```tsx
// src/components/params/ParamsPanel.tsx
import { BlockSizeSlider } from './BlockSizeSlider';
import { AlgorithmToggle } from './AlgorithmToggle';
import { PresetSelector } from './PresetSelector';
import { QuantizeModeSelector } from './QuantizeModeSelector';
import { MaskInvertToggle } from './MaskInvertToggle';

function CompareControl() {
  // ... existing CompareControl code from Phase 1 ...
  const { showCompare, compareMode } = useAppStore((s) => s.ui);
  const setCompareMode = useAppStore((s) => s.setCompareMode);
  const setCompareType = useAppStore((s) => s.setCompareType);

  return (
    <div className="space-y-2">
      <h3 className="text-retro-muted text-xs font-pixel">COMPARE</h3>
      <button
        onClick={() => setCompareMode(!showCompare)}
        className={`w-full py-1.5 text-xs font-terminal border transition-colors ${
          showCompare
            ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
            : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
        }`}
      >
        {showCompare ? '关闭对比' : '开启对比'}
      </button>

      {showCompare && (
        <div className="flex border border-retro-border">
          <button
            onClick={() => setCompareType('split')}
            className={`flex-1 py-1 text-[10px] font-terminal transition-colors ${
              compareMode === 'split'
                ? 'bg-retro-primary text-retro-bg'
                : 'text-retro-muted hover:text-retro-text'
            }`}
          >
            并排
          </button>
          <button
            onClick={() => setCompareType('slider')}
            className={`flex-1 py-1 text-[10px] font-terminal transition-colors border-l border-retro-border ${
              compareMode === 'slider'
                ? 'bg-retro-primary text-retro-bg'
                : 'text-retro-muted hover:text-retro-text'
            }`}
          >
            滑块
          </button>
        </div>
      )}
    </div>
  );
}

export function ParamsPanel() {
  return (
    <div className="w-64 bg-retro-panel border-l-2 border-retro-border p-4 space-y-6 overflow-y-auto">
      <h2 className="text-retro-primary text-xs font-pixel mb-4">PARAMETERS</h2>
      <BlockSizeSlider />
      <AlgorithmToggle />
      <QuantizeModeSelector />
      <PresetSelector />
      <MaskInvertToggle />
      <CompareControl />
    </div>
  );
}
```

- [ ] **Step 5: 运行测试确保无回归**

Run: `npx vitest run`
Expected: 全部测试通过

- [ ] **Step 6: TypeScript 编译检查**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 7: Commit**

```bash
git add src/store/index.ts src/components/params/MaskInvertToggle.tsx src/components/params/ParamsPanel.tsx src/components/canvas/ImageCanvas.tsx
git commit -m "feat: 实现反向选区开关"
```

---

## Task 5: 自定义调色板编辑器

**Files:**
- Create: `src/components/params/CustomPaletteEditor.tsx`
- Modify: `src/components/params/ParamsPanel.tsx`
- Modify: `src/store/index.ts`

**Context:** 提供颜色选择器添加颜色，显示已添加颜色列表（可点击删除），颜色数量限制 2~32。当量化模式为 `fixed-palette` 且未选择预设时，使用自定义调色板。

- [ ] **Step 1: 扩展 Store 添加 customPalette**

```typescript
// src/store/index.ts — 在 AppState 中添加
  // Custom palette
  customPalette: { r: number; g: number; b: number }[];
  addCustomPaletteColor: (color: { r: number; g: number; b: number }) => void;
  removeCustomPaletteColor: (index: number) => void;

// 在 create 的初始值中添加
  customPalette: [],
  addCustomPaletteColor: (color) =>
    set((state) => {
      if (state.customPalette.length >= 32) return state;
      return { customPalette: [...state.customPalette, color] };
    }),
  removeCustomPaletteColor: (index) =>
    set((state) => ({
      customPalette: state.customPalette.filter((_, i) => i !== index),
    })),
```

- [ ] **Step 2: 创建 CustomPaletteEditor 组件**

```tsx
// src/components/params/CustomPaletteEditor.tsx
import { useState, useCallback } from 'react';
import { useAppStore } from '../../store';

function rgbToHex(r: number, g: number, b: number): string {
  return '#' + [r, g, b].map((v) => v.toString(16).padStart(2, '0')).join('');
}

function hexToRgb(hex: string): { r: number; g: number; b: number } | null {
  const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
  return result
    ? {
        r: parseInt(result[1], 16),
        g: parseInt(result[2], 16),
        b: parseInt(result[3], 16),
      }
    : null;
}

export function CustomPaletteEditor() {
  const customPalette = useAppStore((s) => s.customPalette);
  const addColor = useAppStore((s) => s.addCustomPaletteColor);
  const removeColor = useAppStore((s) => s.removeCustomPaletteColor);
  const [colorInput, setColorInput] = useState('#22c55e');

  const handleAdd = useCallback(() => {
    const rgb = hexToRgb(colorInput);
    if (rgb) {
      addColor(rgb);
    }
  }, [colorInput, addColor]);

  return (
    <div className="space-y-2">
      <h3 className="text-retro-muted text-xs font-pixel">CUSTOM PALETTE</h3>

      <div className="flex gap-2 items-center">
        <input
          type="color"
          value={colorInput}
          onChange={(e) => setColorInput(e.target.value)}
          className="w-8 h-8 border border-retro-border cursor-pointer"
        />
        <button
          onClick={handleAdd}
          disabled={customPalette.length >= 32}
          className="px-3 py-1 text-xs font-terminal border border-retro-border text-retro-muted hover:text-retro-primary hover:border-retro-primary disabled:opacity-30 disabled:cursor-not-allowed transition-colors"
        >
          添加
        </button>
        <span className="text-[10px] text-retro-muted font-terminal">
          {customPalette.length}/32
        </span>
      </div>

      {customPalette.length > 0 && (
        <div className="flex flex-wrap gap-1">
          {customPalette.map((color, i) => (
            <button
              key={i}
              onClick={() => removeColor(i)}
              className="w-6 h-6 border border-retro-border hover:border-retro-danger transition-colors"
              style={{ backgroundColor: rgbToHex(color.r, color.g, color.b) }}
              title="点击删除"
            />
          ))}
        </div>
      )}

      {customPalette.length > 0 && (
        <p className="text-[10px] text-retro-muted font-terminal">点击色块删除</p>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 更新 ImageCanvas 使用 customPalette**

```tsx
// src/components/canvas/ImageCanvas.tsx — 在 options 构建逻辑中
// 当 quantizeMethod 为 'fixed-palette' 且没有 presetId 时，使用 customPalette
// 修改后的 options 构建：

      let options: ProcessOptions = {
        downsample: { blockSize: params.blockSize, algorithm: params.algorithm },
        quantize: {
          method: params.quantizeMethod,
          maxColors: params.maxColors,
        },
      };

      if (presetId) {
        const preset = registry.get(presetId);
        if (preset) {
          options = {
            downsample: preset.config.downsample,
            quantize: preset.config.quantize,
          };
        }
      } else if (params.quantizeMethod === 'fixed-palette') {
        const palette = useAppStore.getState().customPalette;
        if (palette.length > 0) {
          options = {
            ...options,
            quantize: {
              method: 'fixed-palette',
              palette,
            },
          };
        }
      }
```

注意：在组件中不要直接使用 `useAppStore.getState()` 在 render 外读取，应该在组件顶部读取：

```tsx
  const customPalette = useAppStore((s) => s.customPalette);
```

然后使用：

```tsx
      } else if (params.quantizeMethod === 'fixed-palette' && customPalette.length > 0) {
        options = {
          ...options,
          quantize: {
            method: 'fixed-palette',
            palette: customPalette,
          },
        };
      }
```

- [ ] **Step 4: 更新 ParamsPanel 添加 CustomPaletteEditor**

```tsx
// src/components/params/ParamsPanel.tsx
import { CustomPaletteEditor } from './CustomPaletteEditor';

// 在 CompareControl 之前添加：
      <CustomPaletteEditor />
```

- [ ] **Step 5: TypeScript 编译检查 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 6: Commit**

```bash
git add src/store/index.ts src/components/params/CustomPaletteEditor.tsx src/components/params/ParamsPanel.tsx src/components/canvas/ImageCanvas.tsx
git commit -m "feat: 实现自定义调色板编辑器"
```

---

## Task 6: 自定义预设保存/加载/删除

**Files:**
- Create: `src/store/storage.ts`
- Modify: `src/store/index.ts`
- Create: `src/components/params/CustomPresetManager.tsx`
- Modify: `src/components/params/ParamsPanel.tsx`

**Context:** 用户可以将当前参数（blockSize, algorithm, quantizeMethod, maxColors, customPalette）保存为命名预设，存入 localStorage。自定义预设出现在预设列表的 "Custom" 分类下，可删除。

- [ ] **Step 1: 创建 localStorage 存储适配器**

```typescript
// src/store/storage.ts
import type { Preset } from '../engine/presets/registry';

const STORAGE_KEY = 'pixelart_custom_presets_v1';

export function loadCustomPresets(): Preset[] {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    return raw ? JSON.parse(raw) : [];
  } catch {
    return [];
  }
}

export function saveCustomPresets(presets: Preset[]): void {
  localStorage.setItem(STORAGE_KEY, JSON.stringify(presets));
}
```

- [ ] **Step 2: 扩展 Store 添加 customPresets**

```typescript
// src/store/index.ts — 在 AppState 中添加
  // Custom presets
  customPresets: Preset[];
  addCustomPreset: (preset: Preset) => void;
  removeCustomPreset: (id: string) => void;

// 导入
import type { Preset } from '../engine/presets/registry';
import { loadCustomPresets, saveCustomPresets } from './storage';

// 在 create 的初始值中添加
  customPresets: loadCustomPresets(),
  addCustomPreset: (preset) =>
    set((state) => {
      const next = [...state.customPresets, preset];
      saveCustomPresets(next);
      return { customPresets: next };
    }),
  removeCustomPreset: (id) =>
    set((state) => {
      const next = state.customPresets.filter((p) => p.id !== id);
      saveCustomPresets(next);
      return { customPresets: next };
    }),
```

- [ ] **Step 3: 创建 CustomPresetManager 组件**

```tsx
// src/components/params/CustomPresetManager.tsx
import { useState, useCallback } from 'react';
import { useAppStore } from '../../store';
import type { Preset } from '../../engine/presets/registry';

export function CustomPresetManager() {
  const params = useAppStore((s) => s.params);
  const customPalette = useAppStore((s) => s.customPalette);
  const customPresets = useAppStore((s) => s.customPresets);
  const addCustomPreset = useAppStore((s) => s.addCustomPreset);
  const removeCustomPreset = useAppStore((s) => s.removeCustomPreset);
  const setPresetId = useAppStore((s) => s.setPresetId);
  const presetId = useAppStore((s) => s.presetId);
  const [name, setName] = useState('');
  const [showInput, setShowInput] = useState(false);

  const handleSave = useCallback(() => {
    if (!name.trim()) return;
    const preset: Preset = {
      id: `custom-${Date.now()}`,
      name: name.trim(),
      category: 'custom',
      config: {
        downsample: {
          blockSize: params.blockSize,
          algorithm: params.algorithm,
        },
        quantize: {
          method: params.quantizeMethod,
          maxColors: params.maxColors,
          palette: customPalette.length > 0 ? customPalette : undefined,
        },
      },
    };
    addCustomPreset(preset);
    setName('');
    setShowInput(false);
  }, [name, params, customPalette, addCustomPreset]);

  return (
    <div className="space-y-2">
      <h3 className="text-retro-muted text-xs font-pixel">CUSTOM PRESETS</h3>

      {!showInput ? (
        <button
          onClick={() => setShowInput(true)}
          className="w-full py-1.5 text-xs font-terminal border border-retro-border text-retro-muted hover:text-retro-primary hover:border-retro-primary transition-colors"
        >
          保存当前参数为预设
        </button>
      ) : (
        <div className="flex gap-2">
          <input
            type="text"
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="预设名称"
            className="flex-1 px-2 py-1 text-xs bg-retro-bg border border-retro-border text-retro-text font-terminal focus:border-retro-primary outline-none"
            onKeyDown={(e) => e.key === 'Enter' && handleSave()}
          />
          <button
            onClick={handleSave}
            className="px-2 py-1 text-xs font-terminal border border-retro-primary text-retro-primary hover:bg-retro-primary hover:text-retro-bg transition-colors"
          >
            保存
          </button>
          <button
            onClick={() => { setShowInput(false); setName(''); }}
            className="px-2 py-1 text-xs font-terminal border border-retro-border text-retro-muted hover:text-retro-text transition-colors"
          >
            取消
          </button>
        </div>
      )}

      {customPresets.length > 0 && (
        <div className="grid grid-cols-2 gap-2">
          {customPresets.map((preset) => (
            <div key={preset.id} className="flex">
              <button
                onClick={() => setPresetId(preset.id)}
                className={`flex-1 py-1.5 px-1 text-[10px] font-terminal border transition-colors text-left ${
                  presetId === preset.id
                    ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
                    : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
                }`}
              >
                {preset.name}
              </button>
              <button
                onClick={() => removeCustomPreset(preset.id)}
                className="w-6 border border-retro-border border-l-0 text-retro-muted hover:text-retro-danger transition-colors text-xs"
              >
                ×
              </button>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 4: 更新 PresetSelector 显示自定义预设**

```tsx
// src/components/params/PresetSelector.tsx — 修改以显示 custom 分类
import { useAppStore } from '../../store';
import { createPresetRegistry } from '../../engine/presets';

const builtInRegistry = createPresetRegistry();

export function PresetSelector() {
  const presetId = useAppStore((s) => s.presetId);
  const setPresetId = useAppStore((s) => s.setPresetId);
  const customPresets = useAppStore((s) => s.customPresets);

  const hardwarePresets = builtInRegistry.getByCategory('hardware');
  const themePresets = builtInRegistry.getByCategory('theme');

  const handleSelect = (id: string) => {
    setPresetId(presetId === id ? null : id);
  };

  return (
    <div className="space-y-3">
      <h3 className="text-retro-muted text-xs font-pixel">PRESETS</h3>

      <div>
        <h4 className="text-[10px] text-retro-muted font-terminal mb-2 uppercase tracking-wider">
          Hardware
        </h4>
        <div className="grid grid-cols-2 gap-2">
          {hardwarePresets.map((preset) => (
            <button
              key={preset.id}
              onClick={() => handleSelect(preset.id)}
              className={`py-2 px-2 text-[10px] font-terminal border transition-colors text-left ${
                presetId === preset.id
                  ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
                  : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
              }`}
            >
              {preset.name}
            </button>
          ))}
        </div>
      </div>

      <div>
        <h4 className="text-[10px] text-retro-muted font-terminal mb-2 uppercase tracking-wider">
          Themes
        </h4>
        <div className="grid grid-cols-2 gap-2">
          {themePresets.map((preset) => (
            <button
              key={preset.id}
              onClick={() => handleSelect(preset.id)}
              className={`py-2 px-2 text-[10px] font-terminal border transition-colors text-left ${
                presetId === preset.id
                  ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
                  : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
              }`}
            >
              {preset.name}
            </button>
          ))}
        </div>
      </div>

      {customPresets.length > 0 && (
        <div>
          <h4 className="text-[10px] text-retro-muted font-terminal mb-2 uppercase tracking-wider">
            Custom
          </h4>
          <div className="grid grid-cols-2 gap-2">
            {customPresets.map((preset) => (
              <button
                key={preset.id}
                onClick={() => handleSelect(preset.id)}
                className={`py-2 px-2 text-[10px] font-terminal border transition-colors text-left ${
                  presetId === preset.id
                    ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
                    : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
                }`}
              >
                {preset.name}
              </button>
            ))}
          </div>
        </div>
      )}

      {presetId && (
        <button
          onClick={() => setPresetId(null)}
          className="w-full py-1.5 text-[10px] font-terminal text-retro-muted border border-retro-border hover:border-retro-warning hover:text-retro-warning transition-colors"
        >
          清除预设
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 5: 更新 ParamsPanel**

```tsx
// src/components/params/ParamsPanel.tsx — 添加 CustomPresetManager
import { CustomPresetManager } from './CustomPresetManager';

// 在 PresetSelector 之后添加：
      <CustomPresetManager />
```

- [ ] **Step 6: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 7: Commit**

```bash
git add src/store/storage.ts src/store/index.ts src/components/params/CustomPresetManager.tsx src/components/params/PresetSelector.tsx src/components/params/ParamsPanel.tsx
git commit -m "feat: 实现自定义预设保存/加载/删除（localStorage 持久化）"
```

---

## Task 7: 人物像素风 — Normal 模式

**Files:**
- Create: `src/engine/person/normal.ts`
- Create: `src/components/params/PersonModeSelector.tsx`
- Modify: `src/store/index.ts`
- Modify: `src/components/canvas/ImageCanvas.tsx`
- Modify: `src/components/params/ParamsPanel.tsx`

**Context:** Normal 模式下人物图片与普通图片处理一致，直接透传 pipeline。需要建立人物模式状态管理和 UI 选择器，为后续 Cutout/Style/Sprite 模式做准备。

- [ ] **Step 1: 扩展 Store 添加 personMode**

```typescript
// src/store/index.ts — 添加
export type PersonMode = 'normal' | 'cutout' | 'style' | 'sprite' | null;

// 在 AppState 中添加
  // Person mode
  personMode: PersonMode;
  setPersonMode: (mode: PersonMode) => void;

// 在 create 初始值中添加
  personMode: null,
  setPersonMode: (mode) => set({ personMode: mode }),
```

- [ ] **Step 2: 创建 person/normal.ts**

```typescript
// src/engine/person/normal.ts
import type { ProcessOptions } from '../types';
import { processImage } from '../pipeline';

export function processNormal(source: ImageData, options: ProcessOptions): ImageData {
  return processImage(source, options);
}
```

- [ ] **Step 3: 创建 PersonModeSelector 组件**

```tsx
// src/components/params/PersonModeSelector.tsx
import { useAppStore } from '../../store';
import type { PersonMode } from '../../store';

const modes: { id: PersonMode; label: string; desc: string }[] = [
  { id: 'normal', label: 'Normal', desc: '普通像素化处理' },
  { id: 'cutout', label: 'Cutout', desc: '人像分割+背景处理' },
  { id: 'style', label: 'Style', desc: '轮廓增强像素风' },
  { id: 'sprite', label: 'Sprite', desc: '多图拼接精灵图' },
];

export function PersonModeSelector() {
  const personMode = useAppStore((s) => s.personMode);
  const setPersonMode = useAppStore((s) => s.setPersonMode);

  return (
    <div className="space-y-2">
      <h3 className="text-retro-muted text-xs font-pixel">PERSON MODE</h3>
      <div className="grid grid-cols-2 gap-2">
        {modes.map((mode) => (
          <button
            key={mode.id}
            onClick={() => setPersonMode(personMode === mode.id ? null : mode.id)}
            className={`py-2 px-1 text-[10px] font-terminal border transition-colors ${
              personMode === mode.id
                ? 'border-retro-primary bg-retro-primary/10 text-retro-primary'
                : 'border-retro-border text-retro-muted hover:border-retro-text hover:text-retro-text'
            }`}
            title={mode.desc}
          >
            {mode.label}
          </button>
        ))}
      </div>
      {personMode && (
        <p className="text-[10px] text-retro-muted font-terminal">
          {modes.find((m) => m.id === personMode)?.desc}
        </p>
      )}
    </div>
  );
}
```

- [ ] **Step 4: 更新 ImageCanvas 支持 personMode**

```tsx
// src/components/canvas/ImageCanvas.tsx — 修改 useEffect 中的处理逻辑
import { processNormal } from '../../engine/person/normal';

// 在组件中添加：
  const personMode = useAppStore((s) => s.personMode);

// 替换 processImage 调用为条件处理：
      let result: ImageData;
      if (personMode === 'normal' || personMode === null) {
        result = processImage(sourceData, options);
      } else {
        // Fallback for now — other modes will be implemented in later tasks
        result = processImage(sourceData, options);
      }
```

- [ ] **Step 5: 更新 ParamsPanel 添加 PersonModeSelector**

```tsx
// src/components/params/ParamsPanel.tsx
import { PersonModeSelector } from './PersonModeSelector';

// 在 MaskInvertToggle 之后添加：
      <PersonModeSelector />
```

- [ ] **Step 6: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 7: Commit**

```bash
git add src/store/index.ts src/engine/person/normal.ts src/components/params/PersonModeSelector.tsx src/components/canvas/ImageCanvas.tsx src/components/params/ParamsPanel.tsx
git commit -m "feat: 实现人物像素风 Normal 模式 + 模式选择器"
```

---

## Task 8: 人物像素风 — Cutout 模式（@mediapipe 人像分割）

**Files:**
- Create: `src/engine/person/cutout.ts`
- Modify: `src/components/canvas/ImageCanvas.tsx`
- Modify: `src/store/index.ts`

**Context:** 使用 @mediapipe/selfie-segmentation 分割人像。由于模型需要从 CDN 加载，实现为异步函数，首次使用时加载模型。背景处理方式：keep（保留原图）/ pixelate（像素化）/ color（纯色填充）。

- [ ] **Step 1: 扩展 Store 添加 cutout 选项**

```typescript
// src/store/index.ts — 在 AppState 中添加
  // Person cutout options
  cutoutBg: 'keep' | 'pixelate' | 'color';
  cutoutBgColor: string;
  setCutoutBg: (bg: 'keep' | 'pixelate' | 'color') => void;
  setCutoutBgColor: (color: string) => void;

// 在 create 初始值中添加
  cutoutBg: 'keep',
  cutoutBgColor: '#0f172a',
  setCutoutBg: (bg) => set({ cutoutBg: bg }),
  setCutoutBgColor: (color) => set({ cutoutBgColor: color }),
```

- [ ] **Step 2: 安装 @mediapipe/selfie-segmentation**

Run: `npm install @mediapipe/selfie-segmentation`

- [ ] **Step 3: 实现 cutout.ts**

```typescript
// src/engine/person/cutout.ts
import type { ProcessOptions, RGB } from '../types';
import { processImage } from '../pipeline';
import { SelfieSegmentation } from '@mediapipe/selfie-segmentation';

let selfieSegmentation: SelfieSegmentation | null = null;
let modelLoading = false;
let modelLoaded = false;

async function ensureModel(): Promise<SelfieSegmentation> {
  if (selfieSegmentation) return selfieSegmentation;
  if (modelLoading) {
    // Wait for existing load
    while (modelLoading) {
      await new Promise((r) => setTimeout(r, 100));
    }
    if (selfieSegmentation) return selfieSegmentation;
  }

  modelLoading = true;
  const ss = new SelfieSegmentation({
    locateFile: (file) => `https://cdn.jsdelivr.net/npm/@mediapipe/selfie-segmentation/${file}`,
  });
  ss.setOptions({ modelSelection: 1 });
  await ss.initialize();
  selfieSegmentation = ss;
  modelLoaded = true;
  modelLoading = false;
  return ss;
}

export function isModelLoaded(): boolean {
  return modelLoaded;
}

export async function processCutout(
  source: ImageData,
  options: ProcessOptions,
  bgMode: 'keep' | 'pixelate' | 'color',
  bgColor: string
): Promise<ImageData> {
  const ss = await ensureModel();

  // Create a canvas to feed to mediapipe
  const canvas = document.createElement('canvas');
  canvas.width = source.width;
  canvas.height = source.height;
  const ctx = canvas.getContext('2d')!;
  ctx.putImageData(source, 0, 0);

  // Run segmentation
  const results = await ss.send({ image: canvas });
  const maskCanvas = results.segmentationMask as HTMLCanvasElement;
  const maskCtx = maskCanvas.getContext('2d')!;
  const maskData = maskCtx.getImageData(0, 0, maskCanvas.width, maskCanvas.height);

  // Process the full image
  const processed = processImage(source, options);

  // Compose based on mask
  const result = new Uint8ClampedArray(source.data);

  // Parse bg color
  let bgR = 15, bgG = 23, bgB = 42;
  if (bgMode === 'color') {
    const hex = bgColor.replace('#', '');
    bgR = parseInt(hex.substring(0, 2), 16);
    bgG = parseInt(hex.substring(2, 4), 16);
    bgB = parseInt(hex.substring(4, 6), 16);
  }

  for (let i = 0; i < source.width * source.height; i++) {
    const maskVal = maskData.data[i * 4]; // Grayscale mask
    const isForeground = maskVal > 128;
    const idx = i * 4;

    if (isForeground) {
      // Foreground: use processed (pixelated person)
      result[idx] = processed.data[idx];
      result[idx + 1] = processed.data[idx + 1];
      result[idx + 2] = processed.data[idx + 2];
    } else {
      // Background: based on mode
      if (bgMode === 'keep') {
        // Keep original background
        // result already has source data
      } else if (bgMode === 'pixelate') {
        result[idx] = processed.data[idx];
        result[idx + 1] = processed.data[idx + 1];
        result[idx + 2] = processed.data[idx + 2];
      } else if (bgMode === 'color') {
        result[idx] = bgR;
        result[idx + 1] = bgG;
        result[idx + 2] = bgB;
      }
    }
  }

  return new ImageData(result, source.width, source.height);
}
```

- [ ] **Step 4: 在 PersonModeSelector 中添加 Cutout 背景选项**

```tsx
// src/components/params/PersonModeSelector.tsx — 在底部添加 Cutout 选项
import { useAppStore } from '../../store';

function CutoutOptions() {
  const cutoutBg = useAppStore((s) => s.cutoutBg);
  const cutoutBgColor = useAppStore((s) => s.cutoutBgColor);
  const setCutoutBg = useAppStore((s) => s.setCutoutBg);
  const setCutoutBgColor = useAppStore((s) => s.setCutoutBgColor);

  return (
    <div className="space-y-2 mt-2 pt-2 border-t border-retro-border">
      <p className="text-[10px] text-retro-muted font-terminal">背景处理</p>
      <div className="flex border border-retro-border">
        {(['keep', 'pixelate', 'color'] as const).map((mode) => (
          <button
            key={mode}
            onClick={() => setCutoutBg(mode)}
            className={`flex-1 py-1 text-[10px] font-terminal transition-colors ${
              cutoutBg === mode
                ? 'bg-retro-primary text-retro-bg'
                : 'text-retro-muted hover:text-retro-text'
            } ${mode !== 'keep' ? 'border-l border-retro-border' : ''}`}
          >
            {mode === 'keep' ? '保留' : mode === 'pixelate' ? '像素化' : '纯色'}
          </button>
        ))}
      </div>
      {cutoutBg === 'color' && (
        <div className="flex items-center gap-2">
          <input
            type="color"
            value={cutoutBgColor}
            onChange={(e) => setCutoutBgColor(e.target.value)}
            className="w-6 h-6 border border-retro-border cursor-pointer"
          />
          <span className="text-[10px] text-retro-muted font-terminal">{cutoutBgColor}</span>
        </div>
      )}
    </div>
  );
}

// 在 PersonModeSelector 组件的 return 中，在模式按钮之后添加：
      {personMode === 'cutout' && <CutoutOptions />}
```

- [ ] **Step 5: 更新 ImageCanvas 支持 Cutout**

```tsx
// src/components/canvas/ImageCanvas.tsx — 在 useEffect 中处理 personMode
import { processCutout } from '../../engine/person/cutout';
import { processNormal } from '../../engine/person/normal';

// 在组件中添加：
  const cutoutBg = useAppStore((s) => s.cutoutBg);
  const cutoutBgColor = useAppStore((s) => s.cutoutBgColor);

// 将 useEffect 改为 async 并用 IIFE：
  useEffect(() => {
    if (!originalImage || !canvasRef.current) return;

    setIsProcessing(true);

    const process = async () => {
      const sourceData = imageToImageData(originalImage);

      let options: ProcessOptions = {
        downsample: { blockSize: params.blockSize, algorithm: params.algorithm },
        quantize: {
          method: params.quantizeMethod,
          maxColors: params.maxColors,
        },
      };

      if (presetId) {
        const preset = registry.get(presetId);
        if (preset) {
          options = {
            downsample: preset.config.downsample,
            quantize: preset.config.quantize,
          };
        }
      } else if (params.quantizeMethod === 'fixed-palette' && customPalette.length > 0) {
        options = {
          ...options,
          quantize: {
            method: 'fixed-palette',
            palette: customPalette,
          },
        };
      }

      let result: ImageData;

      if (personMode === 'cutout') {
        result = await processCutout(sourceData, options, cutoutBg, cutoutBgColor);
      } else {
        result = processImage(sourceData, options);
      }

      // Apply selection mask if shape exists
      if (shape) {
        const mask = createMask(shape, sourceData.width, sourceData.height, invert);
        result = applyMask(sourceData, result, mask);
      }

      const resultCanvas = imageDataToCanvas(result);
      const canvas = canvasRef.current!;
      const ctx = canvas.getContext('2d')!;

      canvas.width = resultCanvas.width;
      canvas.height = resultCanvas.height;
      ctx.drawImage(resultCanvas, 0, 0);

      setIsProcessing(false);
    };

    process();
  }, [originalImage, params, presetId, customPalette, personMode, cutoutBg, cutoutBgColor, setIsProcessing, shape, invert]);
```

注意：需要确保 `customPalette` 也在组件顶部读取了。

- [ ] **Step 6: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 7: Commit**

```bash
git add src/store/index.ts src/engine/person/cutout.ts src/components/params/PersonModeSelector.tsx src/components/canvas/ImageCanvas.tsx package.json
git commit -m "feat: 实现人物像素风 Cutout 模式（@mediapipe 人像分割）"
```

---

## Task 9: 人物像素风 — Style 模式（轮廓增强）

**Files:**
- Create: `src/engine/algorithms/edge-enhance.ts`
- Create: `src/engine/person/style.ts`
- Modify: `src/components/canvas/ImageCanvas.tsx`

**Context:** Style 模式：降采样 + Sobel 边缘检测 + 边缘线增强 + 固定调色板映射。生成更像手绘像素画的效果。

- [ ] **Step 1: 创建 Sobel 边缘检测算法**

```typescript
// src/engine/algorithms/edge-enhance.ts
export function edgeEnhance(source: ImageData, threshold: number = 30): ImageData {
  const { width, height, data } = source;
  const result = new Uint8ClampedArray(data);

  const gray = new Float32Array(width * height);
  for (let i = 0; i < width * height; i++) {
    gray[i] = data[i * 4] * 0.299 + data[i * 4 + 1] * 0.587 + data[i * 4 + 2] * 0.114;
  }

  const sobelX = [-1, 0, 1, -2, 0, 2, -1, 0, 1];
  const sobelY = [-1, -2, -1, 0, 0, 0, 1, 2, 1];

  for (let y = 1; y < height - 1; y++) {
    for (let x = 1; x < width - 1; x++) {
      let gx = 0, gy = 0;
      for (let ky = -1; ky <= 1; ky++) {
        for (let kx = -1; kx <= 1; kx++) {
          const idx = (y + ky) * width + (x + kx);
          const kidx = (ky + 1) * 3 + (kx + 1);
          gx += gray[idx] * sobelX[kidx];
          gy += gray[idx] * sobelY[kidx];
        }
      }
      const magnitude = Math.sqrt(gx * gx + gy * gy);
      const isEdge = magnitude > threshold;

      const idx = (y * width + x) * 4;
      if (isEdge) {
        // Darken edge pixels
        result[idx] = Math.max(0, result[idx] - 60);
        result[idx + 1] = Math.max(0, result[idx + 1] - 60);
        result[idx + 2] = Math.max(0, result[idx + 2] - 60);
      }
    }
  }

  return new ImageData(result, width, height);
}
```

- [ ] **Step 2: 创建 style.ts**

```typescript
// src/engine/person/style.ts
import type { ProcessOptions } from '../types';
import { processImage } from '../pipeline';
import { edgeEnhance } from '../algorithms/edge-enhance';

export function processStyle(source: ImageData, options: ProcessOptions): ImageData {
  // Step 1: Standard pixelization
  let result = processImage(source, options);

  // Step 2: Edge enhancement
  result = edgeEnhance(result, 25);

  return result;
}
```

- [ ] **Step 3: 更新 ImageCanvas 支持 Style**

```tsx
// src/components/canvas/ImageCanvas.tsx — 添加 import
import { processStyle } from '../../engine/person/style';

// 在 process 函数中：
      if (personMode === 'cutout') {
        result = await processCutout(sourceData, options, cutoutBg, cutoutBgColor);
      } else if (personMode === 'style') {
        result = processStyle(sourceData, options);
      } else {
        result = processImage(sourceData, options);
      }
```

- [ ] **Step 4: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 5: Commit**

```bash
git add src/engine/algorithms/edge-enhance.ts src/engine/person/style.ts src/components/canvas/ImageCanvas.tsx
git commit -m "feat: 实现人物像素风 Style 模式（轮廓增强）"
```

---

## Task 10: 人物像素风 — Sprite 模式

**Files:**
- Create: `src/engine/person/sprite.ts`
- Create: `src/components/modals/SpriteSheetModal.tsx`
- Modify: `src/store/index.ts`
- Modify: `src/components/params/PersonModeSelector.tsx`

**Context:** Sprite 模式：用户上传多张图片（正面/侧面/背面等），统一像素化参数处理后拼接为 Sprite Sheet。提供布局选项（水平/网格）和帧间距设置。

- [ ] **Step 1: 扩展 Store 添加 sprite 状态**

```typescript
// src/store/index.ts — 添加
  // Sprite mode
  spriteImages: HTMLImageElement[];
  spriteLayout: 'horizontal' | 'grid';
  spriteGridCols: number;
  spritePadding: number;
  addSpriteImage: (image: HTMLImageElement) => void;
  removeSpriteImage: (index: number) => void;
  clearSpriteImages: () => void;
  setSpriteLayout: (layout: 'horizontal' | 'grid') => void;
  setSpriteGridCols: (cols: number) => void;
  setSpritePadding: (padding: number) => void;

// 在 create 初始值中添加
  spriteImages: [],
  spriteLayout: 'horizontal',
  spriteGridCols: 2,
  spritePadding: 0,
  addSpriteImage: (image) =>
    set((state) => ({ spriteImages: [...state.spriteImages, image] })),
  removeSpriteImage: (index) =>
    set((state) => ({ spriteImages: state.spriteImages.filter((_, i) => i !== index) })),
  clearSpriteImages: () => set({ spriteImages: [] }),
  setSpriteLayout: (layout) => set({ spriteLayout: layout }),
  setSpriteGridCols: (cols) => set({ spriteGridCols: cols }),
  setSpritePadding: (padding) => set({ spritePadding: padding }),
```

- [ ] **Step 2: 创建 sprite.ts**

```typescript
// src/engine/person/sprite.ts
import type { ProcessOptions } from '../types';
import { processImage } from '../pipeline';
import { imageToImageData, imageDataToCanvas } from '../../utils/image';

export interface SpriteSheetOptions {
  layout: 'horizontal' | 'grid';
  gridCols: number;
  padding: number;
}

export function createSpriteSheet(
  images: HTMLImageElement[],
  options: ProcessOptions,
  sheetOptions: SpriteSheetOptions
): HTMLCanvasElement {
  if (images.length === 0) {
    const empty = document.createElement('canvas');
    empty.width = 1;
    empty.height = 1;
    return empty;
  }

  // Process each image with the same options
  const processedCanvases = images.map((img) => {
    const sourceData = imageToImageData(img);
    const result = processImage(sourceData, options);
    return imageDataToCanvas(result);
  });

  const frameW = processedCanvases[0].width;
  const frameH = processedCanvases[0].height;
  const count = processedCanvases.length;

  let cols: number;
  let rows: number;

  if (sheetOptions.layout === 'horizontal') {
    cols = count;
    rows = 1;
  } else {
    cols = sheetOptions.gridCols;
    rows = Math.ceil(count / cols);
  }

  const canvas = document.createElement('canvas');
  canvas.width = cols * frameW + (cols - 1) * sheetOptions.padding;
  canvas.height = rows * frameH + (rows - 1) * sheetOptions.padding;

  const ctx = canvas.getContext('2d')!;

  for (let i = 0; i < count; i++) {
    const col = i % cols;
    const row = Math.floor(i / cols);
    const x = col * (frameW + sheetOptions.padding);
    const y = row * (frameH + sheetOptions.padding);
    ctx.drawImage(processedCanvases[i], x, y);
  }

  return canvas;
}
```

- [ ] **Step 3: 创建 SpriteSheetModal 组件**

```tsx
// src/components/modals/SpriteSheetModal.tsx
import { useState, useCallback } from 'react';
import { useAppStore } from '../../store';
import { createSpriteSheet } from '../../engine/person/sprite';
import { createPresetRegistry } from '../../engine/presets';
import { processImage } from '../../engine';
import { imageToImageData } from '../../utils/image';
import { downloadPNG } from '../../engine/export/png';
import type { ProcessOptions } from '../../engine/types';

const registry = createPresetRegistry();

export function SpriteSheetModal() {
  const [isOpen, setIsOpen] = useState(false);
  const spriteImages = useAppStore((s) => s.spriteImages);
  const addSpriteImage = useAppStore((s) => s.addSpriteImage);
  const removeSpriteImage = useAppStore((s) => s.removeSpriteImage);
  const clearSpriteImages = useAppStore((s) => s.clearSpriteImages);
  const spriteLayout = useAppStore((s) => s.spriteLayout);
  const spriteGridCols = useAppStore((s) => s.spriteGridCols);
  const spritePadding = useAppStore((s) => s.spritePadding);
  const setSpriteLayout = useAppStore((s) => s.setSpriteLayout);
  const setSpriteGridCols = useAppStore((s) => s.setSpriteGridCols);
  const setSpritePadding = useAppStore((s) => s.setSpritePadding);
  const params = useAppStore((s) => s.params);
  const presetId = useAppStore((s) => s.presetId);
  const [previewCanvas, setPreviewCanvas] = useState<HTMLCanvasElement | null>(null);

  const handleFile = useCallback(
    async (file: File) => {
      const img = new Image();
      img.onload = () => addSpriteImage(img);
      img.src = URL.createObjectURL(file);
    },
    [addSpriteImage]
  );

  const handleGenerate = useCallback(() => {
    let options: ProcessOptions = {
      downsample: { blockSize: params.blockSize, algorithm: params.algorithm },
      quantize: { method: params.quantizeMethod, maxColors: params.maxColors },
    };

    if (presetId) {
      const preset = registry.get(presetId);
      if (preset) {
        options = {
          downsample: preset.config.downsample,
          quantize: preset.config.quantize,
        };
      }
    }

    const canvas = createSpriteSheet(spriteImages, options, {
      layout: spriteLayout,
      gridCols: spriteGridCols,
      padding: spritePadding,
    });
    setPreviewCanvas(canvas);
  }, [spriteImages, params, presetId, spriteLayout, spriteGridCols, spritePadding]);

  const handleExport = useCallback(() => {
    if (previewCanvas) {
      downloadPNG(previewCanvas, `spritesheet_${Date.now()}.png`);
    }
  }, [previewCanvas]);

  if (!isOpen) {
    return (
      <button
        onClick={() => setIsOpen(true)}
        className="w-full py-1.5 text-xs font-terminal border border-retro-border text-retro-muted hover:text-retro-primary hover:border-retro-primary transition-colors"
      >
        打开 Sprite Sheet 编辑器
      </button>
    );
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/60">
      <div className="bg-retro-panel border-2 border-retro-border w-[600px] max-h-[80vh] flex flex-col">
        <div className="flex items-center justify-between px-4 py-2 border-b border-retro-border">
          <span className="text-retro-primary text-xs font-pixel">SPRITE SHEET EDITOR</span>
          <button onClick={() => setIsOpen(false)} className="text-retro-muted hover:text-retro-danger text-lg">×</button>
        </div>

        <div className="flex-1 overflow-auto p-4 space-y-4">
          {/* Upload */}
          <div
            onDrop={(e) => {
              e.preventDefault();
              Array.from(e.dataTransfer.files).forEach((f) => handleFile(f));
            }}
            onDragOver={(e) => e.preventDefault()}
            className="border-2 border-dashed border-retro-border p-4 text-center cursor-pointer hover:border-retro-primary transition-colors"
          >
            <p className="text-retro-muted text-xs font-terminal">拖放图片到此处或点击上传</p>
            <input
              type="file"
              multiple
              accept="image/*"
              onChange={(e) => Array.from(e.target.files || []).forEach((f) => handleFile(f))}
              className="hidden"
              id="sprite-upload"
            />
            <label htmlFor="sprite-upload" className="text-retro-primary text-xs font-terminal cursor-pointer">
              选择文件
            </label>
          </div>

          {/* Thumbnails */}
          {spriteImages.length > 0 && (
            <div className="flex gap-2 flex-wrap">
              {spriteImages.map((img, i) => (
                <div key={i} className="relative">
                  <img src={img.src} alt="" className="w-16 h-16 object-contain border border-retro-border" />
                  <button
                    onClick={() => removeSpriteImage(i)}
                    className="absolute -top-1 -right-1 w-4 h-4 bg-retro-danger text-white text-[10px] flex items-center justify-center"
                  >
                    ×
                  </button>
                </div>
              ))}
              <button
                onClick={clearSpriteImages}
                className="px-2 py-1 text-[10px] font-terminal border border-retro-border text-retro-muted hover:text-retro-danger transition-colors"
              >
                清空全部
              </button>
            </div>
          )}

          {/* Layout options */}
          <div className="flex gap-4 items-center">
            <span className="text-[10px] text-retro-muted font-terminal">布局</span>
            <div className="flex border border-retro-border">
              <button
                onClick={() => setSpriteLayout('horizontal')}
                className={`px-3 py-1 text-[10px] font-terminal ${
                  spriteLayout === 'horizontal' ? 'bg-retro-primary text-retro-bg' : 'text-retro-muted'
                }`}
              >
                水平
              </button>
              <button
                onClick={() => setSpriteLayout('grid')}
                className={`px-3 py-1 text-[10px] font-terminal border-l border-retro-border ${
                  spriteLayout === 'grid' ? 'bg-retro-primary text-retro-bg' : 'text-retro-muted'
                }`}
              >
                网格
              </button>
            </div>

            {spriteLayout === 'grid' && (
              <div className="flex items-center gap-2">
                <span className="text-[10px] text-retro-muted font-terminal">列数</span>
                <input
                  type="number"
                  min={1}
                  max={8}
                  value={spriteGridCols}
                  onChange={(e) => setSpriteGridCols(Math.max(1, Math.min(8, Number(e.target.value))))}
                  className="w-12 px-1 py-1 text-xs bg-retro-bg border border-retro-border text-retro-text font-terminal"
                />
              </div>
            )}

            <div className="flex items-center gap-2">
              <span className="text-[10px] text-retro-muted font-terminal">间距</span>
              <input
                type="range"
                min={0}
                max={20}
                value={spritePadding}
                onChange={(e) => setSpritePadding(Number(e.target.value))}
                className="w-20"
              />
              <span className="text-[10px] text-retro-primary font-terminal">{spritePadding}px</span>
            </div>
          </div>

          {/* Generate + Preview */}
          <button
            onClick={handleGenerate}
            disabled={spriteImages.length === 0}
            className="w-full py-2 text-xs font-terminal border border-retro-primary text-retro-primary hover:bg-retro-primary hover:text-retro-bg disabled:opacity-30 disabled:cursor-not-allowed transition-colors"
          >
            生成预览
          </button>

          {previewCanvas && (
            <div className="flex flex-col items-center gap-2">
              <img
                src={previewCanvas.toDataURL()}
                alt="Sprite Sheet Preview"
                className="max-w-full border border-retro-border"
                style={{ imageRendering: 'pixelated' }}
              />
              <button
                onClick={handleExport}
                className="px-4 py-1.5 text-xs font-terminal border border-retro-primary text-retro-primary hover:bg-retro-primary hover:text-retro-bg transition-colors"
              >
                导出 PNG
              </button>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 4: 更新 PersonModeSelector 添加 Sprite 编辑器入口**

```tsx
// src/components/params/PersonModeSelector.tsx — 在底部添加
import { SpriteSheetModal } from '../modals/SpriteSheetModal';

// 在 return 中，在 CutoutOptions 之后添加：
      {personMode === 'sprite' && (
        <div className="mt-2 pt-2 border-t border-retro-border">
          <SpriteSheetModal />
        </div>
      )}
```

- [ ] **Step 5: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 6: Commit**

```bash
git add src/store/index.ts src/engine/person/sprite.ts src/components/modals/SpriteSheetModal.tsx src/components/params/PersonModeSelector.tsx
git commit -m "feat: 实现人物像素风 Sprite 模式 + Sprite Sheet 编辑器"
```

---

## Task 11: 撤销/重做 (Undo/Redo)

**Files:**
- Create: `src/hooks/useHistory.ts`
- Create: `src/components/toolbar/HistoryButtons.tsx`
- Modify: `src/store/index.ts`
- Modify: `src/components/toolbar/Toolbar.tsx`

**Context:** 历史记录保存参数快照（params、selection、presetId、personMode、customPalette）。每次用户"完成一次操作"（松开滑块、完成框选、切换预设）时保存快照。支持 Ctrl+Z / Ctrl+Shift+Z 快捷键。

- [ ] **Step 1: 扩展 Store 添加历史记录**

```typescript
// src/store/index.ts — 添加
export interface HistorySnapshot {
  params: AppState['params'];
  selection: AppState['selection'];
  presetId: string | null;
  personMode: PersonMode;
  customPalette: { r: number; g: number; b: number }[];
}

// 在 AppState 中添加
  // History
  history: {
    past: HistorySnapshot[];
    future: HistorySnapshot[];
  };
  saveSnapshot: () => void;
  undo: () => void;
  redo: () => void;

// 辅助函数：获取当前快照
function getSnapshot(state: AppState): HistorySnapshot {
  return {
    params: { ...state.params },
    selection: { ...state.selection },
    presetId: state.presetId,
    personMode: state.personMode,
    customPalette: [...state.customPalette],
  };
}

function applySnapshot(set: any, snapshot: HistorySnapshot) {
  set({
    params: snapshot.params,
    selection: snapshot.selection,
    presetId: snapshot.presetId,
    personMode: snapshot.personMode,
    customPalette: snapshot.customPalette,
  });
}

// 在 create 初始值中添加
  history: { past: [], future: [] },
  saveSnapshot: () =>
    set((state) => {
      const snapshot = getSnapshot(state);
      // Avoid duplicate consecutive snapshots
      const lastPast = state.history.past[state.history.past.length - 1];
      if (lastPast && JSON.stringify(lastPast) === JSON.stringify(snapshot)) {
        return state;
      }
      return {
        history: {
          past: [...state.history.past, snapshot],
          future: [],
        },
      };
    }),
  undo: () =>
    set((state) => {
      if (state.history.past.length === 0) return state;
      const current = getSnapshot(state);
      const previous = state.history.past[state.history.past.length - 1];
      const newPast = state.history.past.slice(0, -1);
      applySnapshot(set, previous);
      return {
        history: {
          past: newPast,
          future: [current, ...state.history.future],
        },
      };
    }),
  redo: () =>
    set((state) => {
      if (state.history.future.length === 0) return state;
      const current = getSnapshot(state);
      const next = state.history.future[0];
      const newFuture = state.history.future.slice(1);
      applySnapshot(set, next);
      return {
        history: {
          past: [...state.history.past, current],
          future: newFuture,
        },
      };
    }),
```

注意：`applySnapshot` 在 `undo` 和 `redo` 中内部调用了 `set`，这会导致与外部 `set` 冲突。需要重写为单次 set 返回新状态：

```typescript
  undo: () =>
    set((state) => {
      if (state.history.past.length === 0) return state;
      const current = getSnapshot(state);
      const previous = state.history.past[state.history.past.length - 1];
      const newPast = state.history.past.slice(0, -1);
      return {
        params: previous.params,
        selection: previous.selection,
        presetId: previous.presetId,
        personMode: previous.personMode,
        customPalette: previous.customPalette,
        history: {
          past: newPast,
          future: [current, ...state.history.future],
        },
      };
    }),
  redo: () =>
    set((state) => {
      if (state.history.future.length === 0) return state;
      const current = getSnapshot(state);
      const next = state.history.future[0];
      const newFuture = state.history.future.slice(1);
      return {
        params: next.params,
        selection: next.selection,
        presetId: next.presetId,
        personMode: next.personMode,
        customPalette: next.customPalette,
        history: {
          past: [...state.history.past, current],
          future: newFuture,
        },
      };
    }),
```

- [ ] **Step 2: 创建 HistoryButtons 组件**

```tsx
// src/components/toolbar/HistoryButtons.tsx
import { useAppStore } from '../../store';

export function HistoryButtons() {
  const history = useAppStore((s) => s.history);
  const undo = useAppStore((s) => s.undo);
  const redo = useAppStore((s) => s.redo);

  return (
    <div className="flex gap-1">
      <button
        onClick={undo}
        disabled={history.past.length === 0}
        className="w-8 h-8 border border-retro-border flex items-center justify-center text-retro-muted hover:text-retro-primary hover:border-retro-primary disabled:opacity-30 disabled:cursor-not-allowed transition-colors text-xs"
        title="撤销 (Ctrl+Z)"
      >
        ↺
      </button>
      <button
        onClick={redo}
        disabled={history.future.length === 0}
        className="w-8 h-8 border border-retro-border flex items-center justify-center text-retro-muted hover:text-retro-primary hover:border-retro-primary disabled:opacity-30 disabled:cursor-not-allowed transition-colors text-xs"
        title="重做 (Ctrl+Shift+Z)"
      >
        ↻
      </button>
    </div>
  );
}
```

- [ ] **Step 3: 创建 useHistory hook（快捷键）**

```typescript
// src/hooks/useHistory.ts
import { useEffect } from 'react';
import { useAppStore } from '../store';

export function useHistory() {
  const undo = useAppStore((s) => s.undo);
  const redo = useAppStore((s) => s.redo);

  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      if (e.ctrlKey || e.metaKey) {
        if (e.key === 'z') {
          e.preventDefault();
          if (e.shiftKey) {
            redo();
          } else {
            undo();
          }
        }
      }
    };

    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [undo, redo]);
}
```

- [ ] **Step 4: 更新 Toolbar 添加 HistoryButtons**

```tsx
// src/components/toolbar/Toolbar.tsx
import { HistoryButtons } from './HistoryButtons';

// 在 TOOLS 标题之后、工具按钮之前添加：
      <HistoryButtons />
      <div className="w-full h-px bg-retro-border my-1" />
```

- [ ] **Step 5: 在 App.tsx 中挂载 useHistory**

```tsx
// src/App.tsx
import { useHistory } from './hooks/useHistory';

function App() {
  useHistory();
  // ... rest of App
}
```

- [ ] **Step 6: 在关键交互点调用 saveSnapshot**

在以下组件的适当位置调用 `saveSnapshot`：

1. **BlockSizeSlider** — onMouseUp / onChangeComplete（滑块释放时）
2. **AlgorithmToggle** — onClick 时
3. **PresetSelector** — 选择预设时
4. **QuantizeModeSelector** — 模式切换时
5. **MaskInvertToggle** — 切换时
6. **PersonModeSelector** — 模式切换时

由于 `input[type=range]` 的 onChange 在每次拖动时都会触发，我们使用 onMouseUp 来保存快照：

```tsx
// BlockSizeSlider.tsx — 修改 input
      <input
        type="range"
        min={2}
        max={64}
        value={blockSize}
        onChange={(e) => setParams({ blockSize: Number(e.target.value) })}
        onMouseUp={() => saveSnapshot()}
        // ... rest
      />
```

在 BlockSizeSlider 中添加：`const saveSnapshot = useAppStore((s) => s.saveSnapshot);`

类似地修改其他组件。

- [ ] **Step 7: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 8: Commit**

```bash
git add src/store/index.ts src/hooks/useHistory.ts src/components/toolbar/HistoryButtons.tsx src/components/toolbar/Toolbar.tsx src/App.tsx src/components/params/BlockSizeSlider.tsx src/components/params/AlgorithmToggle.tsx src/components/params/PresetSelector.tsx src/components/params/QuantizeModeSelector.tsx src/components/params/MaskInvertToggle.tsx src/components/params/PersonModeSelector.tsx
git commit -m "feat: 实现撤销/重做（Undo/Redo）+ 快捷键支持"
```

---

## Task 12: Web Worker 处理优化

**Files:**
- Create: `src/engine/worker.ts`
- Create: `src/hooks/usePixelEngine.ts`
- Modify: `src/components/canvas/ImageCanvas.tsx`

**Context:** 将像素化处理从主线程移到 Web Worker，避免大图片处理时阻塞 UI。Worker 接收 ImageData + ProcessOptions，返回处理后的 ImageData。使用 `postMessage` + `Transferable` 传递数据。

- [ ] **Step 1: 创建 Worker 入口**

```typescript
// src/engine/worker.ts
import { processImage } from './pipeline';
import type { ProcessOptions } from './types';

self.onmessage = (e: MessageEvent<{ imageData: ImageData; options: ProcessOptions }>) => {
  const { imageData, options } = e.data;
  const result = processImage(imageData, options);
  self.postMessage({ result }, [result.data.buffer]);
};

export {};
```

- [ ] **Step 2: 创建 usePixelEngine hook**

```typescript
// src/hooks/usePixelEngine.ts
import { useRef, useCallback } from 'react';
import type { ProcessOptions } from '../engine/types';

export function usePixelEngine() {
  const workerRef = useRef<Worker | null>(null);

  const getWorker = useCallback(() => {
    if (!workerRef.current) {
      workerRef.current = new Worker(new URL('../engine/worker.ts', import.meta.url), {
        type: 'module',
      });
    }
    return workerRef.current;
  }, []);

  const process = useCallback(
    (imageData: ImageData, options: ProcessOptions): Promise<ImageData> => {
      return new Promise((resolve, reject) => {
        const worker = getWorker();

        const handleMessage = (e: MessageEvent) => {
          worker.removeEventListener('message', handleMessage);
          if (e.data.error) {
            reject(new Error(e.data.error));
          } else {
            resolve(e.data.result);
          }
        };

        worker.addEventListener('message', handleMessage);
        worker.postMessage({ imageData, options }, [imageData.data.buffer]);
      });
    },
    [getWorker]
  );

  return { process };
}
```

注意：Vite 的 Web Worker 语法可能需要调整。如果 `new URL` 方式不工作，改用 inline worker 字符串方式：

```typescript
// src/hooks/usePixelEngine.ts — 备选方案（inline worker）
import { useCallback } from 'react';
import type { ProcessOptions } from '../engine/types';
import { processImage } from '../engine/pipeline';

// Fallback: if Worker fails, process on main thread
function createWorker(): Worker {
  const workerCode = `
    self.onmessage = function(e) {
      const { imageData, options } = e.data;
      // We can't import modules in inline worker, so this approach needs bundler support
      // For Vite, use the new URL approach above
    };
  `;
  const blob = new Blob([workerCode], { type: 'application/javascript' });
  return new Worker(URL.createObjectURL(blob));
}

export function usePixelEngine() {
  const process = useCallback(
    async (imageData: ImageData, options: ProcessOptions): Promise<ImageData> => {
      try {
        const worker = new Worker(new URL('../engine/worker.ts', import.meta.url), {
          type: 'module',
        });
        return new Promise((resolve, reject) => {
          const handleMessage = (e: MessageEvent) => {
            worker.removeEventListener('message', handleMessage);
            worker.terminate();
            if (e.data.error) {
              reject(new Error(e.data.error));
            } else {
              resolve(e.data.result);
            }
          };
          worker.addEventListener('message', handleMessage);
          worker.postMessage({ imageData, options }, [imageData.data.buffer]);
        });
      } catch {
        // Fallback to main thread
        return processImage(imageData, options);
      }
    },
    []
  );

  return { process };
}
```

实际上，Vite 支持 `new Worker(new URL('./worker.ts', import.meta.url), { type: 'module' })` 语法。但如果测试环境（jsdom）不支持 Worker，需要 fallback。

为简化实现，我们采用：

```typescript
// src/hooks/usePixelEngine.ts
import { useCallback } from 'react';
import type { ProcessOptions } from '../engine/types';
import { processImage } from '../engine/pipeline';

let workerInstance: Worker | null = null;

function getWorker(): Worker | null {
  if (typeof Worker === 'undefined') return null;
  if (!workerInstance) {
    try {
      workerInstance = new Worker(new URL('../engine/worker.ts', import.meta.url), {
        type: 'module',
      });
    } catch {
      return null;
    }
  }
  return workerInstance;
}

export function usePixelEngine() {
  const process = useCallback(
    async (imageData: ImageData, options: ProcessOptions): Promise<ImageData> => {
      const worker = getWorker();
      if (!worker) {
        // Fallback to main thread
        return processImage(imageData, options);
      }

      return new Promise((resolve, reject) => {
        const handleMessage = (e: MessageEvent) => {
          worker.removeEventListener('message', handleMessage);
          if (e.data.error) {
            reject(new Error(e.data.error));
          } else {
            resolve(e.data.result);
          }
        };

        worker.addEventListener('message', handleMessage);
        worker.postMessage({ imageData, options }, [imageData.data.buffer]);
      });
    },
    []
  );

  return { process };
}
```

- [ ] **Step 3: 更新 ImageCanvas 使用 Web Worker**

```tsx
// src/components/canvas/ImageCanvas.tsx — 添加 import
import { usePixelEngine } from '../../hooks/usePixelEngine';

// 在组件中添加：
  const { process: processInWorker } = usePixelEngine();

// 在 useEffect 中，将 processImage 调用替换为 processInWorker：
      let result: ImageData;

      if (personMode === 'cutout') {
        result = await processCutout(sourceData, options, cutoutBg, cutoutBgColor);
      } else if (personMode === 'style') {
        result = processStyle(sourceData, options);
      } else {
        result = await processInWorker(sourceData, options);
      }
```

注意：`processInWorker` 返回 Promise，所以需要 `await`。

- [ ] **Step 4: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 5: Commit**

```bash
git add src/engine/worker.ts src/hooks/usePixelEngine.ts src/components/canvas/ImageCanvas.tsx
git commit -m "feat: Web Worker 像素化处理优化"
```

---

## Task 13: 缩略图预览优化

**Files:**
- Create: `src/hooks/useThumbnail.ts`
- Modify: `src/components/canvas/ImageCanvas.tsx`
- Modify: `src/store/index.ts`

**Context:** 用户调整参数时，先用缩略图（最长边 800px）生成预览，保证 < 200ms 响应。0.5s 无操作后再用原图生成高质量版本。需要创建缩略图生成 hook，并在 ImageCanvas 中管理"预览质量"状态。

- [ ] **Step 1: 创建缩略图生成工具**

```typescript
// src/hooks/useThumbnail.ts
import { useCallback } from 'react';

const THUMBNAIL_MAX_SIZE = 800;

export function createThumbnail(img: HTMLImageElement): HTMLCanvasElement {
  const canvas = document.createElement('canvas');
  const scale = Math.min(1, THUMBNAIL_MAX_SIZE / Math.max(img.naturalWidth, img.naturalHeight));
  canvas.width = Math.round(img.naturalWidth * scale);
  canvas.height = Math.round(img.naturalHeight * scale);
  const ctx = canvas.getContext('2d')!;
  ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
  return canvas;
}

export function useThumbnail() {
  const generateThumbnail = useCallback((img: HTMLImageElement): HTMLCanvasElement => {
    return createThumbnail(img);
  }, []);

  return { generateThumbnail };
}
```

- [ ] **Step 2: 扩展 Store 添加 thumbnail**

```typescript
// src/store/index.ts — 添加
  // Thumbnail
  thumbnailImage: HTMLCanvasElement | null;
  setThumbnailImage: (canvas: HTMLCanvasElement | null) => void;

// 初始值
  thumbnailImage: null,
  setThumbnailImage: (canvas) => set({ thumbnailImage: canvas }),
```

- [ ] **Step 3: 更新 UploadZone 生成缩略图**

```tsx
// src/components/canvas/UploadZone.tsx — 修改 handleFile
import { createThumbnail } from '../../hooks/useThumbnail';

// 在 handleFile 中，设置 originalImage 后生成缩略图：
      try {
        const img = await loadImage(file);
        setOriginalImage(img);
        const thumb = createThumbnail(img);
        useAppStore.getState().setThumbnailImage(thumb);
      } catch {
        alert('图片加载失败');
      }
```

注意：在组件内部不要直接使用 `useAppStore.getState()`。应该在组件顶部读取 `setThumbnailImage`：

```tsx
  const setThumbnailImage = useAppStore((s) => s.setThumbnailImage);
```

然后：

```tsx
        const thumb = createThumbnail(img);
        setThumbnailImage(thumb);
```

- [ ] **Step 4: 更新 ImageCanvas 支持缩略图预览**

```tsx
// src/components/canvas/ImageCanvas.tsx
import { useEffect, useRef, useState, useCallback } from 'react';
import { useAppStore } from '../../store';
import { processImage } from '../../engine';
import { createPresetRegistry } from '../../engine/presets';
import { imageToImageData, imageDataToCanvas } from '../../utils/image';
import { createMask, applyMask } from '../../engine/selection/mask';
import type { Shape } from '../../engine/selection/shapes';
import { SelectionOverlay } from './SelectionOverlay';
import type { SelectionTool } from './SelectionOverlay';
import { processCutout } from '../../engine/person/cutout';
import { processStyle } from '../../engine/person/style';
import { usePixelEngine } from '../../hooks/usePixelEngine';

const registry = createPresetRegistry();

interface ImageCanvasProps {
  selectionTool?: SelectionTool;
}

export function ImageCanvas({ selectionTool }: ImageCanvasProps) {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const originalImage = useAppStore((s) => s.originalImage);
  const thumbnailImage = useAppStore((s) => s.thumbnailImage);
  const params = useAppStore((s) => s.params);
  const presetId = useAppStore((s) => s.presetId);
  const customPalette = useAppStore((s) => s.customPalette);
  const invert = useAppStore((s) => s.selection.invert);
  const personMode = useAppStore((s) => s.personMode);
  const cutoutBg = useAppStore((s) => s.cutoutBg);
  const cutoutBgColor = useAppStore((s) => s.cutoutBgColor);
  const setIsProcessing = useAppStore((s) => s.setIsProcessing);
  const [shape, setShape] = useState<Shape | null>(null);
  const { process: processInWorker } = usePixelEngine();

  // Debounced high-quality processing
  const hqTimeoutRef = useRef<ReturnType<typeof setTimeout> | null>(null);

  const processWithSource = useCallback(async (source: HTMLImageElement | HTMLCanvasElement) => {
    if (!canvasRef.current) return;

    const sourceData = source instanceof HTMLImageElement
      ? imageToImageData(source)
      : source.getContext('2d')!.getImageData(0, 0, source.width, source.height);

    let options = {
      downsample: { blockSize: params.blockSize, algorithm: params.algorithm },
      quantize: {
        method: params.quantizeMethod,
        maxColors: params.maxColors,
      },
    };

    if (presetId) {
      const preset = registry.get(presetId);
      if (preset) {
        options = {
          downsample: preset.config.downsample,
          quantize: preset.config.quantize,
        };
      }
    } else if (params.quantizeMethod === 'fixed-palette' && customPalette.length > 0) {
      options = {
        ...options,
        quantize: {
          method: 'fixed-palette',
          palette: customPalette,
        },
      };
    }

    let result: ImageData;

    if (personMode === 'cutout') {
      result = await processCutout(sourceData, options, cutoutBg, cutoutBgColor);
    } else if (personMode === 'style') {
      result = processStyle(sourceData, options);
    } else {
      result = await processInWorker(sourceData, options);
    }

    if (shape) {
      const mask = createMask(shape, sourceData.width, sourceData.height, invert);
      result = applyMask(sourceData, result, mask);
    }

    const resultCanvas = imageDataToCanvas(result);
    const canvas = canvasRef.current;
    const ctx = canvas.getContext('2d')!;

    canvas.width = resultCanvas.width;
    canvas.height = resultCanvas.height;
    ctx.drawImage(resultCanvas, 0, 0);
  }, [params, presetId, customPalette, personMode, cutoutBg, cutoutBgColor, shape, invert, processInWorker]);

  useEffect(() => {
    if (!originalImage || !canvasRef.current) return;

    setIsProcessing(true);

    // Use thumbnail for quick preview
    const source = thumbnailImage || originalImage;

    requestAnimationFrame(() => {
      processWithSource(source).then(() => {
        setIsProcessing(false);

        // Schedule high-quality render with original image after 500ms of no changes
        if (hqTimeoutRef.current) {
          clearTimeout(hqTimeoutRef.current);
        }
        hqTimeoutRef.current = setTimeout(() => {
          if (thumbnailImage && canvasRef.current) {
            setIsProcessing(true);
            processWithSource(originalImage).then(() => {
              setIsProcessing(false);
            });
          }
        }, 500);
      });
    });

    return () => {
      if (hqTimeoutRef.current) {
        clearTimeout(hqTimeoutRef.current);
      }
    };
  }, [originalImage, thumbnailImage, processWithSource, setIsProcessing]);

  if (!originalImage) return null;

  return (
    <div className="relative inline-block">
      <canvas
        ref={canvasRef}
        className="max-w-full max-h-full object-contain"
        style={{ imageRendering: 'pixelated' }}
      />
      <SelectionOverlay
        width={originalImage.naturalWidth}
        height={originalImage.naturalHeight}
        tool={selectionTool ?? null}
        onShapeCreated={setShape}
      />
    </div>
  );
}
```

注意：`processWithSource` 在依赖数组中，而 `useEffect` 的依赖应该精简。由于 `processWithSource` 已经包含了所有参数依赖，`useEffect` 只需要监听 `originalImage`、`thumbnailImage` 和 `processWithSource`。

- [ ] **Step 5: TypeScript 编译 + 运行测试**

Run: `npx tsc --noEmit && npx vitest run`
Expected: 无错误，全部测试通过

- [ ] **Step 6: Commit**

```bash
git add src/hooks/useThumbnail.ts src/store/index.ts src/components/canvas/UploadZone.tsx src/components/canvas/ImageCanvas.tsx
git commit -m "feat: 实现缩略图预览优化（800px 快速预览 + 原图高质量延迟渲染）"
```

---

## Task 14: 代码审查与回归测试

**Files:**
- 全部修改过的文件

- [ ] **Step 1: 运行全部测试**

Run: `npx vitest run`
Expected: 全部测试通过

- [ ] **Step 2: TypeScript 编译检查**

Run: `npx tsc --noEmit`
Expected: 无错误

- [ ] **Step 3: Build 检查**

Run: `npm run build`
Expected: 构建成功

- [ ] **Step 4: Dev 服务器功能验证**

Run: `npm run dev`
验证以下功能：
1. 多边形框选：点击添加顶点，双击闭合
2. 手绘框选：拖拽绘制，自动闭合
3. 反向选区：开关切换，实时生效
4. 自定义调色板：添加/删除颜色，颜色数量限制
5. 自定义预设：保存当前参数，命名，localStorage 持久化，删除
6. 人物模式选择器：Normal/Cutout/Style/Sprite 切换
7. Cutout：模型加载，人像分割，背景处理（保留/像素化/纯色）
8. Style：轮廓增强效果
9. Sprite：多图上传，布局选择，生成预览，导出
10. 撤销/重做：按钮和快捷键
11. Web Worker：大图片处理不阻塞 UI
12. 缩略图：快速预览 + 延迟高质量渲染

- [ ] **Step 5: 更新文档**

更新 `docs/prd/2026-05-25-像素风图片转换.md` 的 Phase 2 检查清单，将已完成功能标记为 ✅。
更新 `docs/specs/2026-05-25-技术设计.md` 添加 Phase 2 架构说明。
更新 `.wolf/anatomy.md` 添加新文件。

- [ ] **Step 6: 最终 Commit**

```bash
git add docs/ .wolf/
git commit -m "docs: 更新 Phase 2 技术文档和文件索引"
```

---

## 自审检查

### 1. Spec 覆盖度

| PRD 需求 | 实现任务 |
|----------|----------|
| F-02 图片裁剪 | Phase 2 未包含（P2 优先级） |
| F-03 框选工具 — 多边形、自由手绘 | Task 1-3 ✅ |
| F-04 反向选区 | Task 4 ✅ |
| F-09 自定义调色板 | Task 5 ✅ |
| F-09 自定义预设保存/加载/删除 | Task 6 ✅ |
| F-11 人物像素风 — Normal | Task 7 ✅ |
| F-11 人物像素风 — Cutout | Task 8 ✅ |
| F-11 人物像素风 — Style | Task 9 ✅ |
| F-11 人物像素风 — Sprite | Task 10 ✅ |
| F-12 Sprite Sheet 导出 | Task 10 ✅ |
| F-13 撤销/重做 | Task 11 ✅ |
| 性能 — Web Worker | Task 12 ✅ |
| 性能 — 缩略图预览 | Task 13 ✅ |

### 2. Placeholder 扫描

- 无 "TBD"、"TODO"、"implement later"
- 所有代码步骤包含完整代码
- 所有测试包含完整断言

### 3. 类型一致性

- `SelectionTool` 类型在 App.tsx、Toolbar.tsx、SelectionOverlay.tsx 中一致
- `Shape` 类型扩展后向下兼容 Rectangle/Circle
- `ProcessOptions` 在所有调用点结构一致
- `Preset` 接口包含 `category: 'custom'`

---

## 执行交接

**Plan complete and saved to `docs/plans/2026-05-25-像素风图片转换-phase2.md`.**

本计划包含 14 个 Task，覆盖 PixelArt Studio Phase 2（P1 增强功能）：

| Task | 内容 | 预估时间 |
|------|------|----------|
| Task 1 | 扩展 Shape 类型（多边形+手绘） | 15 min |
| Task 2 | 扩展 Mask 引擎 | 20 min |
| Task 3 | SelectionOverlay 扩展 | 30 min |
| Task 4 | 反向选区 | 15 min |
| Task 5 | 自定义调色板编辑器 | 20 min |
| Task 6 | 自定义预设保存/加载/删除 | 25 min |
| Task 7 | 人物 Normal 模式 | 15 min |
| Task 8 | 人物 Cutout 模式 | 35 min |
| Task 9 | 人物 Style 模式 | 20 min |
| Task 10 | 人物 Sprite 模式 + Sprite Sheet | 40 min |
| Task 11 | 撤销/重做 | 25 min |
| Task 12 | Web Worker 优化 | 20 min |
| Task 13 | 缩略图预览优化 | 20 min |
| Task 14 | 审查与回归测试 | 20 min |

**总计：约 5-6 小时**

**Two execution options:**

**1. Subagent-Driven (recommended)** — 每个 Task 分派独立子代理执行，任务间有代码审查，适合保证质量

**2. Inline Execution** — 在当前会话中批量执行，带检查点审查，速度更快

**Which approach?**
