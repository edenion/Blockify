# OpenWolf

@.wolf/OPENWOLF.md

本项目使用 OpenWolf 进行上下文管理。每次会话请阅读并遵循 .wolf/OPENWOLF.md。生成代码前请查看 .wolf/cerebrum.md。读取文件前请查看 .wolf/anatomy.md。

## 助手行为约束

1. **默认使用中文回复**：除非用户明确要求使用其他语言，否则所有回复均使用中文。
2. **基于最新网络资源**：回答问题时，优先检索并使用最新的网络资源，确保信息的时效性和准确性。

## 提交与 Pull Request 规范

### 提交信息

- 使用英文前缀 + 中文内容，例如：`fix: 修复登录失效问题`、`style: 调整按钮边距`
- 常见前缀：`fix:`、`style:`、`feat:`、`refactor:`、`docs:`、`test:`、`chore:`
- 提交信息使用简短祈使句，每次提交只解决一个明确问题
- 避免混入无关改动

## 文档规范

### 文档结构

本项目维护两类核心文档，每次需求变更或功能迭代时须同步更新：

| 文档 | 路径 | 定位 | 更新时机 |
|------|------|------|----------|
| **PRD（产品需求文档）** | `docs/prd/2026-05-25-像素风图片转换.md` | 面向产品和功能：用户故事、验收标准、非功能需求、风险、迭代路线图 | 需求变更、新增功能、调整优先级时 |
| **技术设计文档** | `docs/specs/2026-05-25-技术设计.md` | 面向开发：架构、算法、数据流、文件结构、扩展性设计 | 架构调整、新增模块、接口变更时 |

### 文档维护要求

- **需求变更必须同步更新 PRD**：新增/删除功能、调整优先级、修改验收标准时，先在 PRD 中更新，再开始编码
- **架构变更必须同步更新技术设计文档**：新增模块、修改接口、调整数据流时，先在设计文档中更新
- **文档与代码保持一致**：任何文档中描述的内容必须在代码中有对应实现；代码中新增的公共接口必须在文档中补充说明
- **更新后更新 anatomy.md**：文档文件发生变更（新增、删除、重命名）时，同步更新 `.wolf/anatomy.md` 中的索引
- **CLAUDE.md 持续维护**：本项目规范、约束、流程发生变更时，须同步更新 `CLAUDE.md`。CLAUDE.md 是项目指令的权威来源，必须与实际执行保持一致

### Pull Request

- 包含：变更说明、影响模块、关联 issue 或方案文档、测试结果
- **如涉及需求变更，须注明对应的 PRD 更新内容**
- **如涉及架构变更，须注明对应的技术设计文档更新内容**
- 不要提交真实 `.env` 配置
- 机器本地路径和凭据应保留在本地环境，不得进入版本控制
